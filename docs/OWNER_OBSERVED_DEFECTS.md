# Owner-observed defects — the register that stops an observation disappearing

## What this is, and what it is not

This is **not** a second requirements system. `docs/REQUIREMENTS_TRACEABILITY_MATRIX.md` remains the
only authority on what this product owes; every row below maps to an ID that already exists there.

It is a **loss-prevention register**. The owner reports a defect against the running product, work
starts, a pull request lands, a gate goes green — and somewhere in that sequence the original
observation stops being tracked as an observation and starts being tracked as an implementation
detail. Then a status report says a requirement is done while the owner is still looking at the
thing they reported. That is the failure this file exists to prevent, and it has already happened
here more than once.

## The rules

- **An observation is never deleted.** It is closed only by acceptance on the running Production
  product, by the owner or by a check against the real authenticated surface.
- **Every row maps to an existing Matrix ID.** Where none fits, the row is marked
  `UNMAPPED_OWNER_DEFECT` and reconciled with the Matrix *before* any implementation.
- **Merged is not deployed. Deployed is not verified.** The four columns are separate because they
  have been confused, and a green test suite is none of them.
- **A fallback sentence is not a fix** where the underlying capability can still be made to work.
  Telling a reader why a picture is missing is the right behaviour when it genuinely is; it is not
  the answer when the picture could have been shown.

## Status of the columns

`Matrix status` is read from the Matrix and is the canonical one — this file never overrides it.
`Prod verified` is the only column that closes a row.

| # | Sev | Owner observation | Symptom | Route | Matrix ID(s) | Matrix status | Root cause | Code | Merged | Deployed | Prod verified | Remaining acceptance gap | External blocker |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 1 | P0 | /app/content previews still do not visibly render reliably | Cards on the content library show nothing where an ad should be | /app/content | AD-PREVIEW-001; CONTENT-PREVIEW-SHAPES-001 | AD-PREVIEW-001=PARTIAL<br>CONTENT-PREVIEW-SHAPES-001=IN_PROGRESS | Meta share links rendered as images (see 3); catalog ads mis-typed (see 8) | yes (#299) | yes (#299) | yes (in `origin/main`, deployed 125333db) | no | Owner-authenticated /app/content must show media in AR/EN, 1440/390, 3 browsers | — |
| 2 | P0 | Meta blank image cards | Blank rectangle, no explanation | /app/content | AD-PREVIEW-001 | AD-PREVIEW-001=PARTIAL | `CreativePresenter` read the image as `asset_url ?? preview_url`; `preview_url` is Meta `preview_shareable_link` | yes (#299) | yes (#299) | yes (in `origin/main`, deployed 125333db) | **yes** | Re-probe after deploy must show 0 unusable PRODUCTION VERIFIED 2026-09-08 (media probe, run 34280538217): usable stills 12, unusable 0, no still 0. Every asset HTTP 200, `image/jpeg`, real decoded dimensions (1290x2793, 1152x2048, 64x64). The stated gap was «re-probe after deploy must show 0 unusable» — met. | — |
| 3 | P0 | fb.me/shareable HTML must never be rendered as an image | `<img>` fetched 260KB of `text/html`, decoded nothing | /app/content; report ad detail | AD-PREVIEW-001 | AD-PREVIEW-001=PARTIAL | PROVEN: 6 of 12 Meta first-page cards fetched fb.me at HTTP 200 `text/html` | yes (#299) | yes (#299) | yes (in `origin/main`, deployed 125333db) | partial (report surface) | Production probe after deploy PRODUCTION VERIFIED 2026-09-08 on the client report (`c51228b8`): 3 images all decoded (1080x1920) with zero broken, zero images fetching `fb.me`/shareable HTML, 3 coverless films each carrying «فيديو — لم ترسل المنصة صورة غلاف له. افتح الإعلان لتشغيله.» and zero empty absence boxes, and no literal «undefined» or NaN anywhere in the rendered page. This is the REPORT surface only — /app/content remains owner-acceptance-open, and geometry claims were NOT taken from this reading because the browser pane reported `document.hidden`, which makes every measured width unreliable. Server half RECONFIRMED on META 2026-09-08 as well as Snapchat: all 12 first-page assets return `image/jpeg`, zero fetch HTML. The remaining half is owner-visible geometry, which this agent cannot measure — the browser pane renders at `docW: 0`. | — |
| 4 | P0 | Videos must actually play | A film with no cover showed a grey box | /app/content | AD-PREVIEW-001; CONTENT-PREVIEW-SHAPES-001 | AD-PREVIEW-001=PARTIAL<br>CONTENT-PREVIEW-SHAPES-001=IN_PROGRESS | `absenceLabel` had no branch for video-without-poster; grid mounts `VideoPoster` **WEBKIT, FOUND 2026-09-07 BY THE NEW GATE.** `VideoPoster` reported a painted frame from the `seeked` EVENT, and WebKit does not always send one: under `preload="metadata"` with no user gesture it accepts the `currentTime` and defers the decode until something plays, so the seek never completes and the card stays empty. On CI`s WebKit this poster sat at `data-painted=false` for twenty seconds on the very file the same browser plays perfectly one route away — a blank rectangle of exactly the kind the owner reports. It now settles on `readyState >= HAVE_CURRENT_DATA`, which is the browser`s own statement that a frame exists however it arrived, and nudges the decode with a muted inline `play()` stopped on the first frame. Two attempts to MAKE WebKit decode failed on CI, and the second failure was the instructive one: the element simply stayed mounted at `data-painted=false`, so the card was an empty box for ever. The requirement is not «every engine manages a poster» — it is owner row 10, no unexplained blank rectangle. So the attempt has an 8s budget and, when it expires, the card is told the asset is unavailable and falls back to «فيديو — لم ترسل المنصة صورة غلاف له», the same true sentence the client report has shown for this shape all along. A browser that can decode still shows the frame. | yes (earlier) | yes | yes | IMPLEMENTED_NOT_VERIFIED | Owner-visible playback on the real surface CODE COMPLETE AND DEPLOYED (`c51228b8`). The WebKit cause is fixed and proved on CI: the poster now paints in 2.7s on the browser that sat blank for twenty seconds across two earlier attempts. Owner acceptance on the authenticated /app/content remains OPEN — no authenticated Production session is available to this agent, and the owner has not yet named a failing card. | — |
| 5 | P0 | Story/vertical media must render at the correct aspect | 9:16 cropped into 16:9 lost the logo and CTA | /app/content | CONTENT-PREVIEW-SHAPES-001 | CONTENT-PREVIEW-SHAPES-001=IN_PROGRESS | Every card was `aspect-video`; provider dimensions were synced but unused | yes (earlier) | yes | yes | no | Owner-visible check at 1440 and 390 | — |
| 6 | P0 | Carousel navigation must work | A five-card creative rendered as one picture | /app/content; content detail | CONTENT-PREVIEW-SHAPES-001; CREATIVE-AD-RELATION-001 | CONTENT-PREVIEW-SHAPES-001=IN_PROGRESS<br>CREATIVE-AD-RELATION-001=PARTIAL | `cards` column was never written by any importer | yes (earlier) | yes | yes | no | Owner-visible carousel paging | — |
| 7 | P0 | Collection media must render where provider data exists | Collection hero absent; tiles never fetched | /app/content | CONTENT-PREVIEW-SHAPES-001 | CONTENT-PREVIEW-SHAPES-001=IN_PROGRESS | `shape_not_fetched` state: Snapchat exposes tiles, the product does not fetch them | partial (probe --shapes) | yes | yes | no | Tiles are still not fetched — the gap is ours and is stated on the card. Blocked on OBSERVATION, not on the platform: the tile payload cannot be guessed, `raw` is not stored for Snapchat creatives, and no operator report could say whether the live estate holds a collection at all. `integrations:diagnose --hierarchy` now prints CREATIVE SHAPES — every stated format with how many of its rows carry no asset link of any kind — so the next Production read answers «is there a live subject» before any fetch is written against a shape nobody has seen. LIVE SUBJECT CONFIRMED 2026-09-07 by the shape census deployed in `5a6da0c2`: the live Snapchat account holds 434 collection creatives, of which 4 carry no asset link of any kind. So this is no longer speculative — there are real collection cards drawing nothing, and the tile fetch has a subject to be written against. UNBLOCKED 2026-09-08: `integrations:probe --structure --shapes` reads the ads edge and reports the KEY NAMES each creative body carries, grouped by the platform`s own type, with no values at all. The first attempt assumed the structure sweep already held the creatives and printed nothing — `syncCampaigns()` reads only the campaigns edge — so the ads edge is now asked explicitly behind a flag. Guarded both ways, including a test proving no name, id or media reference from the body reaches the output. | — |
| 8 | P0 | Multi-asset/catalog/product media must render where provider data exists | Catalog ads read as images with a missing picture | /app/content | CONTENT-PREVIEW-SHAPES-001 | CONTENT-PREVIEW-SHAPES-001=IN_PROGRESS | Meta reports `object_type: SHARE` for a DPA; `object_story_spec.template_data` is the real marker | yes (#299) | yes (#299) | yes (in `origin/main`, deployed 125333db) | no | Forced Meta structure sync must re-map the rows to `catalog` | — |
| 9 | P0 | No broken-image icon | A refused asset left a broken frame | /app/content | AD-PREVIEW-001 | AD-PREVIEW-001=PARTIAL | `<img>` had no `onError` on any surface | yes (#297) | yes | yes | partial (report surface) | Owner-visible confirmation PRODUCTION VERIFIED 2026-09-08 on the client report (`c51228b8`): 3 images all decoded (1080x1920) with zero broken, zero images fetching `fb.me`/shareable HTML, 3 coverless films each carrying «فيديو — لم ترسل المنصة صورة غلاف له. افتح الإعلان لتشغيله.» and zero empty absence boxes, and no literal «undefined» or NaN anywhere in the rendered page. This is the REPORT surface only — /app/content remains owner-acceptance-open, and geometry claims were NOT taken from this reading because the browser pane reported `document.hidden`, which makes every measured width unreliable. | — |
| 10 | P0 | No unexplained blank rectangle | Empty card, no sentence, no state | /app/content | AD-PREVIEW-001 | AD-PREVIEW-001=PARTIAL | Same as 9; plus the grid used one generic sentence for every shape | yes (#297,#299) | partial | yes (in `origin/main`, deployed 125333db) | partial (report surface) | Owner-visible confirmation PRODUCTION VERIFIED 2026-09-08 on the client report (`c51228b8`): 3 images all decoded (1080x1920) with zero broken, zero images fetching `fb.me`/shareable HTML, 3 coverless films each carrying «فيديو — لم ترسل المنصة صورة غلاف له. افتح الإعلان لتشغيله.» and zero empty absence boxes, and no literal «undefined» or NaN anywhere in the rendered page. This is the REPORT surface only — /app/content remains owner-acceptance-open, and geometry claims were NOT taken from this reading because the browser pane reported `document.hidden`, which makes every measured width unreliable. | — |
| 11 | P0 | Expired media must RECOVER, not merely say expired | Stale link drew nothing | /app/content | AD-MEDIA-RECOVERY-001 | AD-MEDIA-RECOVERY-001=PARTIAL | `asset_expires_at` was never emitted by any connector, so `expired` was unreachable | yes (#295,#297) | yes | yes | no | Recovery proven as a chain in test; not yet observed on a real expired Production row | — |
| 12 | P0 | Content detail modal must use the same canonical preview truth | Detail disagreed with the card | content detail | CONTENT-DETAIL-MODAL-001; AD-PREVIEW-001 | CONTENT-DETAIL-MODAL-001=IMPLEMENTED_NOT_VERIFIED<br>AD-PREVIEW-001=PARTIAL | Six surfaces each re-derived the rules; now one `readPreview` | yes (earlier) | yes | yes | no | Owner-visible parity check | — |
| 13 | P0 | Literal "undefined" must never appear as a currency | «237.90 undefined» on screen | /app/analytics objectives; family decision table | NUMBER-PRESENTATION-001 | NUMBER-PRESENTATION-001=PARTIAL | `moneyExact` interpolated a bare `${currency}`; two call sites passed none; `FamilyDecisionTable` typed the formatter as one-argument | yes (#298) | yes (#298) | yes (in `origin/main`, deployed 125333db) | no | Swept on Production 2026-09-10 on the client report: ZERO literal «undefined» and zero «NaN» in the rendered page. Authenticated surfaces still unswept. | — |
| 14 | P0 | Large numbers must use canonical compact notation | «1,284,663 SAR» beside a card reading «1.28M SAR»; and on the LIVE client report the KPI card read «6.6M» while the funnel bar beneath it read «6,596,500» | /app/analytics; entity tables; shared report funnel | NUMBER-PRESENTATION-001; CLIENT-FACING-PRESENTATION-001 | NUMBER-PRESENTATION-001=**PARTIAL**<br>CLIENT-FACING-PRESENTATION-001=**PARTIAL** | Family summary fallbacks called `toLocaleString`; `ConversionFunnelChart` called `num()`, the EXACT formatter, on the most public surface in the product | yes (#298) | yes | yes (ecef0bd0) | **yes** | — zero full-width counts on the live client report; funnel reads 6.66M / 40.4K / 14.6K / 4.54K with exact titles | — |
| 15 | P0 | Decision-critical values must keep meaningful precision | A CPM of 29.71 printed «30 SAR» | KPI strip; reports | NUMBER-PRESENTATION-001 | NUMBER-PRESENTATION-001=PARTIAL | `moneyExact` rounded through `num()` | yes (earlier) | yes | yes | no | **A real loss was found and fixed 2026-09-10.** The direct-vs-blended block rounded a cost per order to whole units — «17 USD» against a payload of 17.62, beside a KPI card reading 17.62 — because it used the compact reader where every other cost-per used the exact one. Fixed and guarded. Small money elsewhere on the report keeps its decimals. Owner-visible confirmation on authenticated surfaces still outstanding. | — |
| 16 | P0 | Numeric headers and values aligned in RTL and LTR | Figures sat under the wrong heading | analytical tables | TABLE-NUMERIC-ALIGNMENT-001 | TABLE-NUMERIC-ALIGNMENT-001=PARTIAL | Sweep measures drift on 7 tabs + hand-rolled tables | yes | yes | yes | partial | Alignment half is measured on one column only on this seed | — |
| 17 | P0 | KPI cards must use one canonical geometry | Cards differed per surface | dashboard; analytics; reports | UX-KPI-PRESENTATION-001 | UX-KPI-PRESENTATION-001=PARTIAL | — | partial | yes | yes | no | Owner-visible confirmation across surfaces | — |
| 18 | P0 | KPI movement and real sparkline where evidence exists | Cards showed no trend | dashboard; analytics | UX-KPI-PRESENTATION-001 | UX-KPI-PRESENTATION-001=PARTIAL | — | partial | yes | yes | no | Owner-visible confirmation | — |
| 19 | P0 | No page-level mobile horizontal overflow | Page scrolled sideways at 390 | all | TABLE-NUMERIC-ALIGNMENT-001; TYPOGRAPHY-PRODUCT-POLISH-001 | TABLE-NUMERIC-ALIGNMENT-001=PARTIAL<br>TYPOGRAPHY-PRODUCT-POLISH-001=PARTIAL | Most recent: my own 384px reservation on a 390px screen | yes | yes (#302) | yes (in `origin/main`, deployed 125333db) | no | Covered by the appearance gate; owner check at 390 | — |
| 20 | P0 | Real Meta + Snapchat Project 1 data must reconcile end-to-end | Figures disagreed between surfaces | all | PROVIDER-CROSS-SURFACE-PROPAGATION-001; CROSS-PLATFORM-ATTRIBUTION-DEPTH-001 | PROVIDER-CROSS-SURFACE-PROPAGATION-001=IN_PROGRESS<br>CROSS-PLATFORM-ATTRIBUTION-DEPTH-001=PARTIAL | — | partial | partial | partial | no | Surface-level reconciliation on live data not yet done | — |
| 21 | P0 | All-provider filter = Meta + Snapchat | — | /app/analytics | ANALYTICS-FILTER-TRUTH-001 | ANALYTICS-FILTER-TRUTH-001=IN_PROGRESS | — | yes | — | — | no | Held end to end by `provider-filter-scopes.spec.ts` on three browsers: the REQUEST carries `provider=meta` and the rows narrow to Meta alone, and «All» is proved a real state by returning to the full set. Proved by injection — dropping the parameter from the query fails it. Awaiting the same proof on live Production data. | — |
| 22 | P0 | Meta filter = Meta only | — | /app/analytics | ANALYTICS-FILTER-TRUTH-001 | ANALYTICS-FILTER-TRUTH-001=IN_PROGRESS | — | yes | — | — | no | Held end to end by `provider-filter-scopes.spec.ts` on three browsers: the REQUEST carries `provider=meta` and the rows narrow to Meta alone, and «All» is proved a real state by returning to the full set. Proved by injection — dropping the parameter from the query fails it. Awaiting the same proof on live Production data. | — |
| 23 | P0 | Snapchat filter = Snapchat only | — | /app/analytics | ANALYTICS-FILTER-TRUTH-001 | ANALYTICS-FILTER-TRUTH-001=IN_PROGRESS | — | yes | — | — | no | Held end to end by `provider-filter-scopes.spec.ts` on three browsers: the REQUEST carries `provider=meta` and the rows narrow to Meta alone, and «All» is proved a real state by returning to the full set. Proved by injection — dropping the parameter from the query fails it. Awaiting the same proof on live Production data. | — |
| 24 | P0 | Provider totals → aggregate → API → Dashboard → Analytics → Reports reconcile | — | all | PROVIDER-CROSS-SURFACE-PROPAGATION-001 | PROVIDER-CROSS-SURFACE-PROPAGATION-001=IN_PROGRESS | — | partial | — | — | no | Independent recomputation vs each surface outstanding | — |
| 25 | P0 | Derived ratios recomputed, never averaged or summed | CTR/CPC/ROAS averaged from provider ratios | aggregation | CROSS-PLATFORM-ATTRIBUTION-DEPTH-001 | CROSS-PLATFORM-ATTRIBUTION-DEPTH-001=PARTIAL | `MetricsAggregator::withDerived()` recomputes; `CrossProviderTotalsAreTruthfulTest` holds it | yes | yes | yes | **yes** | Held at the SURFACE by `derived-ratios-recomputed.spec.ts` on three browsers: the four objective KPIs form a closed identity — `roas × cpa × purchases = revenue`, spend cancels — which holds only if every one came from the same aggregate numerator and denominator. Proved by injection: a ROAS that does not come from the totals fails it and the message names the arithmetic. `CrossProviderTotalsAreTruthfulTest` holds the same rule where it is computed; this holds it where it is read. PRODUCTION VERIFIED 2026-09-08: every derived ratio on the live client report recomputed independently from the aggregate numerator and denominator and matched to stated precision — ctr 40205/6601746=0.00609, cpm 1.49, cpa 9842.78/566=17.39, cpc 0.245, roas 4.086. Spend reconciles across totals, platforms, objective paths and the funnel at 9,842.78, and conversions at 566. None averaged, none summed from per-row ratios. | — |
| 26 | P0 | Purchases/Leads/Installs/Registrations/Conversations not one Results total | — | analytics; reports | CROSS-PLATFORM-ATTRIBUTION-DEPTH-001 | CROSS-PLATFORM-ATTRIBUTION-DEPTH-001=PARTIAL | `CampaignObjective::path()` files Leads, App installs, Add to cart, Sales, Conversions and Purchases on the SAME conversion path, so `ObjectivePerformance` summed their `orders` into one figure and derived one `cpa` from it — a lead programme`s price averaged with a sale`s, always downwards because leads are many and cheap. `LiveDetailTables`, `InteractiveReport` and `PrintDocument` all print that figure to a client. | yes | — | yes (5a6da0c2) | **yes** | PRODUCTION VERIFIED 2026-09-07 on the owner`s own client link, visibly. The conversion path really was mixed: 582 results shown as one number over 350 conversions and 232 sales, priced at a single «تكلفة النتيجة» of 17 USD across both. The detailed objectives table now renders «· مزيج» beside that figure carrying «النتائج هنا أكثر من نوع واحد (التحويلات 350 · المبيعات 232)، فتكلفة النتيجة متوسط بين أنواع مختلفة ولا تصلح لقياس نوع بعينه». The awareness and traffic paths, one kind of result each, stay unmarked. | — |
| 27 | P0 | Cross-platform Reach must not claim deduplicated unique people | The Reach hint read «عدد الأشخاص المختلفين الذين رأوا الإعلان» / «How many different people saw the ad» — over a figure that is usually a cross-platform SUM | analytics; reports | CROSS-PLATFORM-ATTRIBUTION-DEPTH-001 | CROSS-PLATFORM-ATTRIBUTION-DEPTH-001=PARTIAL | Reach is de-duplicated by each platform WITHIN ITSELF and by nobody after that. Meta cannot tell Snapchat who it reached and this product cannot either, so the sum counts anybody reached on both twice. The claim lived in `metricCatalog`, which every surface reads its copy from. | yes | — | — | no | The hint now states the figure in both scopes — each platform`s own count, added and not de-duplicated across them. Guarded on the CATALOGUE, since that is the one place the claim can be made; proved by injection in AR and EN. Needs merge, deploy and an owner-visible check of the tooltip. | — |
| 28 | P0 | Platform-attributed revenue distinct from store-confirmed | — | analytics; reports | CROSS-PLATFORM-ATTRIBUTION-DEPTH-001; MONEY-SCOPE-TRUTH-001 | CROSS-PLATFORM-ATTRIBUTION-DEPTH-001=PARTIAL<br>MONEY-SCOPE-TRUTH-001=VERIFIED | — | partial | — | — | no | CHECKED 2026-09-07 at the catalogue every surface reads its copy from: revenue is labelled «قيمة الطلبات» with «قيمة المبيعات كما تقدّرها بكسلات المنصات» / «Sales value as the platforms’ pixels estimate it», and purchases «عمليات الشراء التي أبلغت بها المنصات — لا سجل المتجر» / «not the store’s own ledger». Neither claims to be store-confirmed. What is still owed is the same check on a project that HAS a store, where the two figures sit side by side and the distinction has teeth. | — |
| 29 | P0 | Incomplete currency/coverage shows partial/withheld truth | A withheld figure printed as 0 | all money surfaces | MONEY-SCOPE-TRUTH-001; MONEY-USD-002 | MONEY-SCOPE-TRUTH-001=VERIFIED<br>MONEY-USD-002=PARTIAL | — | yes | yes | yes | partial | Historical re-normalisation still PARTIAL | — |
| 30 | P1 | Client-facing reports simple and professional | — | shared report | CLIENT-FACING-PRESENTATION-001 | CLIENT-FACING-PRESENTATION-001=PARTIAL | — | partial | — | — | no | — | — |
| 31 | P1 | Operator diagnostics must not appear in client payload/UI/PDF/email | — | shared report; PDF; email | CLIENT-DIAGNOSTIC-SEPARATION-001 | CLIENT-DIAGNOSTIC-SEPARATION-001=PARTIAL | Found on the owner`s LIVE client link 2026-09-07 by walking the payload`s key names. `totals.coverage`, `totals.spend_coverage` and `totals.revenue_coverage` each carried `reasons` — `AggregateCoverage``s own «contributor → human-readable evidence», written for an operator. One of them read, in English on an Arabic client report: «The last sync failed: No connector is registered for provider `sandbox`.» An internal exception, the name of an internal artefact, and the implication that the client`s figures hang on our plumbing. | yes | — | yes (5fa2efa1) | **yes** | PRODUCTION VERIFIED 2026-09-07 after `5fa2efa1`: `reasons` is empty on every coverage block on the owner`s live link, and neither «No connector is registered» nor «The last sync failed» appears anywhere in the payload. The verdict survived — the block still reads `partial` and still names linkedin, meta and snapchat as included — so the client is still told their figures are incomplete, without being handed our account of why. | — |
| 32 | P1 | Internal campaign/ad-set/ad names must not leak to client reports | — | shared report | CLIENT-DIAGNOSTIC-SEPARATION-001 | CLIENT-DIAGNOSTIC-SEPARATION-001=PARTIAL | The NAMES were withheld and guarded; the primary KEYS were not. 22 internal UUIDs counted on the owner`s live link 2026-09-07 as `ads[].id`, `ads[].campaign_id` and the same pair inside `ads_groups[].ads[]` — a stable identifier for the very entity whose name the link refuses to print. The snapshot path had unset `campaign_id` since it was written; the live path never learned the rule. | yes | — | yes (5fa2efa1) | **yes** | PRODUCTION VERIFIED 2026-09-07 after `5fa2efa1`: `id` and `campaign_id` are gone from `ads` and from `ads_groups[].ads`, and no whole-value UUID appears anywhere in the payload. The ads still carry name, preview, provider and their metrics. NOTE on the measurement: a first check reported «17 UUIDs still present» and was WRONG — it grepped the encoded JSON with an unanchored pattern and matched hex runs inside the providers` own signed CDN links (11 of the 17 sit inside a URL). The guard in `LiveReportShareTest` had the same flaw and now walks the structure testing each VALUE, which is the actual claim; re-proved by reverting the fix. | — |
| 33 | P1 | Detailed Report detailed through the supported hierarchy | — | reports | REPORT-DETAIL-PARITY-001 | REPORT-DETAIL-PARITY-001=PARTIAL | — | partial | — | — | no | — | — |
| 34 | P1 | Content/media previews must render inside reports | On the LIVE client report the three HIGHEST-SPENDING ad cards showed «جُلب هذا الإعلان من المنصة، ولم تُتِح المنصة أصل المحتوى» where a picture belongs | reports | REPORT-AD-PREVIEW-001 | REPORT-AD-PREVIEW-001=VERIFIED | All three are Snapchat COMPOSITE creatives — story ads. A composite carries no `top_snap_media_id` of its own; it names its children in `composite_properties.creative_ids`, and the connector only ever read the direct field, so it stored nothing and blamed the platform for it. | yes | yes | yes (711a5f69) | **yes** | — forced Snapchat sweep 2026-09-07 11:18 `success records=11852`; the three composite cards that said «ولم تُتِح المنصة أصل المحتوى» now carry media: one renders an `<img>`, and the two films say «فيديو — لم ترسل المنصة صورة غلاف له. افتح الإعلان لتشغيله» and play. The false accusation against Snapchat is gone. | — |
| 35 | P1 | Budget/funnel/store/findings/recommendations appear where the form requires | — | reports | REPORT-DETAIL-PARITY-001; REPORT-ANALYTICAL-DEPTH-001 | REPORT-DETAIL-PARITY-001=PARTIAL<br>REPORT-ANALYTICAL-DEPTH-001=PARTIAL | — | partial | — | — | no | — | — |
| 36 | P1 | Snapshot and Live form parity truthful | — | reports | REPORT-INTERACTION-PARITY-001 | REPORT-INTERACTION-PARITY-001=VERIFIED | — | yes | yes | yes | no | — | — |
| 37 | P1 | Report creation exposes mode/form/section/branding choices | — | report creation | REPORT-CREATION-UX-001 | REPORT-CREATION-UX-001=PARTIAL | — | partial | — | — | no | — | — |
| 38 | P1 | Client/project branding and logo must actually render | — | reports; portal | BRANDING-RENDER-EVIDENCE-001; BRANDING-HIERARCHY-001 | BRANDING-RENDER-EVIDENCE-001=IN_PROGRESS<br>BRANDING-HIERARCHY-001=PARTIAL | — | partial | — | — | no | Per-surface render evidence outstanding | — |
| 39 | P1 | Share/PDF/report/email identity and branding agree | — | share; PDF; email | BRANDING-HIERARCHY-001; REPORT-TITLE-METADATA-001 | BRANDING-HIERARCHY-001=PARTIAL<br>REPORT-TITLE-METADATA-001=IN_PROGRESS | — | partial | — | — | no | — | — |
| 40 | P1 | Canonical filters scope backend aggregations | — | analytics; dashboard | ANALYTICS-FILTER-TRUTH-001; DASH-010 | ANALYTICS-FILTER-TRUTH-001=IN_PROGRESS<br>DASH-010=PARTIAL | — | yes | — | — | no | Held end to end by `provider-filter-scopes.spec.ts` on three browsers: the REQUEST carries `provider=meta` and the rows narrow to Meta alone, and «All» is proved a real state by returning to the full set. Proved by injection — dropping the parameter from the query fails it. Awaiting the same proof on live Production data. | — |
| 41 | P1 | Filters persist through refresh/back/deep-link | The library forgot the chosen view | /app/content; analytics | ANALYTICS-FILTER-TRUTH-001 | ANALYTICS-FILTER-TRUTH-001=IN_PROGRESS | `view` was state with no seed and no write-back; the address is now the only source of truth | yes | yes (#302) | yes (in `origin/main`, deployed 125333db) | no | Held end to end by `filters-survive.spec.ts` on three browsers — refresh, back, and a COLD deep link, which is what a shared link is. Proved by injection: writing the address but never reading it back fails the refresh case. Awaiting deploy for Production confirmation. | — |
| 42 | P1 | Objective system drives KPIs/columns/rankings consistently | — | analytics | ANALYTICS-OBJECTIVE-SYSTEM-001; OBJECTIVE-ANALYTICS-DEPTH-001 | ANALYTICS-OBJECTIVE-SYSTEM-001=IMPLEMENTED_NOT_VERIFIED<br>OBJECTIVE-ANALYTICS-DEPTH-001=PARTIAL | — | partial | — | — | no | — | — |
| 43 | P1 | Analytics materially different from Dashboard | Both rendered one composition | /app/dashboard; /app/analytics | ANALYTICS-DIFFERENTIATION-001 | ANALYTICS-DIFFERENTIATION-001=IMPLEMENTED_NOT_VERIFIED | `PerformanceTab` received no `surface` | yes | yes | yes | no | Owner-visible check of both routes | — |
| 44 | P1 | Platforms gives within-objective decision analytics | — | analytics platforms | PLATFORM-DECISION-ANALYTICS-001; PLATFORM-DECISION-ANALYTICS-001-UI | PLATFORM-DECISION-ANALYTICS-001=PARTIAL<br>PLATFORM-DECISION-ANALYTICS-001-UI=PARTIAL | — | partial | — | — | no | — | — |
| 45 | P1 | Campaign workspace active-first and decision-oriented | — | /app/campaigns | CAMPAIGN-INTELLIGENCE-HUB | CAMPAIGN-INTELLIGENCE-HUB=IN_PROGRESS | — | partial | — | — | no | — | — |
| 46 | P1 | Campaign → ad set → ad → content drill-down preserves context | — | analytics; campaigns | HIERARCHY-ENTITY-ANALYTICS-DRILLDOWN | HIERARCHY-ENTITY-ANALYTICS-DRILLDOWN=IMPLEMENTED_NOT_VERIFIED | — | yes | yes | yes | no | — | — |
| 47 | P1 | Large-estate selection/search usable | — | filters | UX-MULTISELECT-SCALE-001 | UX-MULTISELECT-SCALE-001=IN_PROGRESS | — | partial (scope picker scaled) | — | — | no | The analytics filters were wired for scale; the REPORT SCOPE picker still laid every option out as a flat chip — accounts, campaigns, ad sets, ads and content, on an estate holding hundreds. Those five axes now use the product`s shared `MultiSelectField` (search, select-all, removable chips), the same control every other multi-select surface uses. Guarded at the requirement`s own bar: 200 campaigns, found by typing, selection landing as `{campaign_ids:[c137]}`. Remaining: 3-browser evidence at that cardinality. | — |
| 48 | P1 | Connection state must not imply data-flow health | HEALTHY while contributing nothing | /app/integrations | INTEG-ESTATE-001; PROVIDER-CROSS-SURFACE-PROPAGATION-001 | INTEG-ESTATE-001=VERIFIED<br>PROVIDER-CROSS-SURFACE-PROPAGATION-001=IN_PROGRESS | `AccountHealth` never asked whether a row was stored; `NO_DATA` added | yes | yes | yes | no | Owner-visible check | — |
| 49 | P1 | Manage Accounts works without unnecessary re-OAuth | — | /app/integrations | INTEGRATION-DATASOURCE-WIZARD-001 | INTEGRATION-DATASOURCE-WIZARD-001=PARTIAL | — | partial | — | — | no | — | — |
| 50 | P1 | Sandbox rows must never reach live providers | sbx-cmp-* written into live accounts | /app/integrations | SANDBOX-PROD-001 | SANDBOX-PROD-001=IMPLEMENTED_NOT_VERIFIED | `ProjectIntegrationController::sync()` ran the sandbox connector against real bindings | yes | yes | yes | **yes** | Owner-visible check **PRODUCTION VERIFIED 2026-09-09 (`8316d3a7`).** The client report`s spend coverage now reads `state: complete` with `expected: [meta, linkedin, snapchat]`, `excluded: []` — `sandbox` is gone. Real contributors intact and real figures untouched: totals 9,842.78 spend and 566 conversions, unchanged, with the platform breakdown still snapchat`s own 9,842.78. Nothing was deleted or hidden.  The rows themselves still exist and their removal stays AUTHORIZED, NOT EXECUTED (row 51) — this closes the client-visible falsehood, not the data cleanup. | — |
| 51 | P1 | Historical sandbox contamination visible and safely handled | — | /app/integrations | SANDBOX-PROD-001 | SANDBOX-PROD-001=IMPLEMENTED_NOT_VERIFIED | Census separates contaminated rows by `raw.sandbox`, never by name | yes | yes | yes | partial | Quarantine remains report-only by design DRY RUN 2026-09-07 on Production: 1 live account carries sandbox rows — the Snapchat account — and it carries exactly 2 of 89 stored campaigns, `sbx-cmp-1 Sandbox Awareness` and `sbx-cmp-2 Sandbox Conversions`. They are the reason the owner`s client report declares its spend coverage `partial`: `ContributorCoverage::expectedProviders()` reads `external_campaigns.provider`, so a contaminated row makes `sandbox` an expected contributor, and having no connector it can only fail. OWNER AUTHORISED the removal on 2026-09-07. The workflow deliberately cannot pass `--apply` — its own note says removing production rows must not be doable by ticking a box — so the apply is owed from a VPS shell: `docker compose --env-file deploy/backend.production.env -f deploy/docker-compose.production.yml exec -T backend php artisan integrations:quarantine-sandbox --provider=snapchat --apply`. Expected after: 2 rows removed, 87 stored, and the client`s spend coverage stops reading `partial`. NOTE 2026-09-09: the client-visible symptom is now fixed in code (`8316d3a7`) — a fixture row no longer counts as a contributor, so coverage reads `complete`. The two rows remain in the database and their removal is still owed; it is no longer urgent, because nothing customer-facing depends on it. | — |
| 52 | P1 | Budget monitoring must never imply provider enforcement | — | /app/budget | BUDGET-GOVERNANCE-001 | BUDGET-GOVERNANCE-001=PARTIAL | — | partial | — | — | no | CHECKED 2026-09-07: the spend-limit surface states «هذا حد داخلي للمراقبة والتنبيه — لا يوقف عرض الإعلانات على أي منصة» / «an internal limit for watching and warning — it does not stop delivery on any ad platform», in both languages, and `SpendLimitsPage.test.tsx` holds it. No change needed. | — |
| 53 | P1 | Budget hierarchy/threshold/email workflow incomplete until verified | — | /app/budget | BUDGET-GOVERNANCE-001; BUDGET-ALERT-EMAIL-001 | BUDGET-GOVERNANCE-001=PARTIAL<br>BUDGET-ALERT-EMAIL-001=IMPLEMENTED_NOT_VERIFIED | — | partial | — | — | no | — | — |
| 54 | P1 | Data Quality operator-actionable | — | /app/analytics data quality | DATA-QUALITY-OPERATOR-UX-001 | DATA-QUALITY-OPERATOR-UX-001=PARTIAL | — CREATIVE SHAPES added to `--hierarchy`: the report counted 1,451 creatives and could not tell a drawable one from a shape with no picture at all. | partial | — | — | no | — | — |
| 55 | P1 | Store-confirmed and platform-attributed outcomes separated | — | analytics; reports | CROSS-PLATFORM-ATTRIBUTION-DEPTH-001 | CROSS-PLATFORM-ATTRIBUTION-DEPTH-001=PARTIAL | — | partial | — | — | no | — | — |
| 56 | P1 | Performance email a compact actionable dashboard | — | email | EMAIL-DASHBOARD-UX-001 | EMAIL-DASHBOARD-UX-001=PARTIAL | — | partial | — | — | no | — | — |
| 57 | P1 | Daily/weekly/monthly semantics correct | — | email | EXECUTIVE-DAILY-DIGEST-001; DIGEST-PORTFOLIO-SCOPE-001 | EXECUTIVE-DAILY-DIGEST-001=IMPLEMENTED_NOT_VERIFIED<br>DIGEST-PORTFOLIO-SCOPE-001=IMPLEMENTED_NOT_VERIFIED | — | yes | yes | yes | no | — | — |
| 58 | P1 | Live email sending not verified without a real delivery | — | email | EMAIL-SETTINGS-DEPTH-001 | EMAIL-SETTINGS-DEPTH-001=IN_PROGRESS | — | partial | — | — | no | — | BLOCKED_OPERATIONAL_EVIDENCE — needs a real delivery |
| 59 | P1 | Project RBAC enforced on backend and completed in management UI | — | team; projects | TEAM-PROJECT-RBAC-001 | TEAM-PROJECT-RBAC-001=IN_PROGRESS | — | partial | — | — | no | **A real hole was found and closed 2026-09-10.** The metrics family — 18 routes — was exempted from `ProjectRouteCapabilityCoverageTest` on a stated reason that was false: `authorizeView` checked the TENANT `campaigns.view` and narrowed nothing. A lead agent read a project's spend, revenue, campaigns, platforms, funnel and entity drill-down (200) while the budget routes beside them refused the same reader (403). `authorizeView` now resolves through `ProjectAbilities`; exemptions are checked rather than believed, which found four dead entries. Owner-visible acceptance of a narrowed member's session remains. | — |
| 60 | P1 | Executive Ops frontend/drilldown incomplete | — | executive ops | EXECUTIVE-OPS-DASHBOARD-001 | EXECUTIVE-OPS-DASHBOARD-001=IMPLEMENTED_NOT_VERIFIED | — | partial | — | — | no | — | — |
| 61 | EXT | Google Ads Basic Access awaiting Google approval | CHECKED ON PRODUCTION 2026-09-15 13:40 UTC via `integrations:diagnose` (read-only): `No external account matches that filter` for provider `google` — zero Google ad accounts have ever been discovered on production. Asked twice, because the first run used the workflow's own suggested key `google_ads`, which matches no row and answers identically to «this provider has no accounts»; the second used `google`, the key the data uses, and agreed. So the API-level approval cannot be taken further here: `ListAccessibleCustomers`, the MCC hierarchy, `login-customer-id` and a real sync all need an account to be connected first, and none is | integrations | INTEGRATION-GOOGLE-001 | INTEGRATION-GOOGLE-001=BLOCKED_EXTERNAL_CREDENTIALS | — | n/a | n/a | n/a | n/a | — | BLOCKED_EXTERNAL_CREDENTIALS |
| 62 | EXT | Google Brand Verification external workflow | — | integrations | INTEGRATION-GOOGLE-001 | INTEGRATION-GOOGLE-001=BLOCKED_EXTERNAL_CREDENTIALS | — | n/a | n/a | n/a | n/a | — | BLOCKED_EXTERNAL_CREDENTIALS |
| 63 | EXT | TikTok production approval external where pending | — | integrations | INTEGRATION-TIKTOK-001; TIKTOK-001 | INTEGRATION-TIKTOK-001=BLOCKED_EXTERNAL_CREDENTIALS<br>TIKTOK-001=BLOCKED_EXTERNAL_CREDENTIALS | — | n/a | n/a | n/a | n/a | — | BLOCKED_EXTERNAL_CREDENTIALS |
| 64 | EXT | WhatsApp conversation identity needs WhatsApp Business authorization | — | leads | LEAD-SOURCE-ATTRIBUTION-001 | LEAD-SOURCE-ATTRIBUTION-001=VERIFIED | Meta Ads messaging metrics and WhatsApp conversations are different sources | n/a | n/a | n/a | n/a | — | BLOCKED_EXTERNAL_CREDENTIALS |
| 65 | EXT | Missing real email delivery evidence is operational | — | email | EMAIL-SETTINGS-DEPTH-001 | EMAIL-SETTINGS-DEPTH-001=IN_PROGRESS | — | n/a | n/a | n/a | n/a | — | BLOCKED_OPERATIONAL_EVIDENCE |
| 68 | OPS | A sync run left «running» by a vanished worker blocks operations and misreports the pipeline | A forced Snapchat structure sync was refused by its own guard: a run had been «running» since 2026-08-26 — twelve days, on a job whose timeout is fifteen minutes | integrations; the accept command; the Integration Centre | PROVIDER-CROSS-SURFACE-PROPAGATION-001; INTEG-SYNC-001 | PROVIDER-CROSS-SURFACE-PROPAGATION-001=**IN_PROGRESS**<br>INTEG-SYNC-001=**VERIFIED** | `SyncAccountStructureJob::failed()` closes a run whose job the QUEUE knows died; it is never called when the worker itself vanishes (SIGKILL, OOM, a container replaced mid-flight), so the row stays open with no upper bound | yes | yes | yes (ce055c71) | **yes** | — the run open since 2026-08-26 is closed: the accept command`s stale-run guard no longer fires and a forced sweep queued and finished `success` in 52s. | |
| 69 | OPS | A live Snapchat binding reads `connection=error` | `Primemode` → project «henka», bound and ACTIVE, with `connection=error` and a single metrics run from 2026-09-06 18:17. Its provider campaigns are 4. | integrations | INTEG-ESTATE-001; PROVIDER-CROSS-SURFACE-PROPAGATION-001 | INTEG-ESTATE-001=**VERIFIED**<br>PROVIDER-CROSS-SURFACE-PROPAGATION-001=**IN_PROGRESS** | Not yet traced. The connection is in error at the provider or credential layer, which is the owner's to resolve; the product's job is to make it visible and not to imply the project is fed. | n/a | n/a | n/a | no | Needs the owner's credentials. Registered so it is not lost; the product-side question is whether the Integration Centre states «connected but no data» for this binding rather than implying health. | BLOCKED_EXTERNAL_CREDENTIALS |
| 70 | OPS | The Snapchat estate grew from 309 discovered accounts with 1 active binding to **619 with 3** | Two further projects are now bound («henka» and one more). Nothing broke; recorded because every earlier trace in these documents assumed one bound account, and a reader comparing them would otherwise think a number had drifted. | integrations | INTEG-ESTATE-001 | INTEG-ESTATE-001=**VERIFIED** | Owner action, not a defect | n/a | n/a | n/a | n/a | — | |
| 71 | P0 | Analytics navigation carried both «الإعلانات» (Ads) and «الإعلان» (Ad) — two tabs, one entity level | Two sibling tabs whose names differ by a plural — «الإعلانات» and «الإعلان» — so a reader could not tell which answered their question | /app/analytics; /agency/analytics | ADS-TERMINOLOGY-001; CONTENT-TERMINOLOGY-001; HIERARCHY-ENTITY-ANALYTICS-DRILLDOWN | ADS-TERMINOLOGY-001=**PARTIAL**<br>HIERARCHY-ENTITY-ANALYTICS-DRILLDOWN=**IMPLEMENTED_NOT_VERIFIED** | A NAMING fault, not a redundant surface. The second tab is the last rung of campaign → ad set → ad → CONTENT and owns the content grain: `creative_daily_metrics`, `contentIntelligence`, `ContentReading`, ad/ad-set scoped content analysis. My first attempt deleted it and aliased its address to Ads, which lost the capability and answered a content link with an ads table. THEN, post-#302: the rename reached the TAB only. Inside it the panel still read «أداء الإعلانات», the first column «الإعلان», the description «from ad-level data» and the empty state «No ad was reported» — over rows whose figures are `creative_daily_metrics` at the content grain. | yes | yes (#302) | yes (in `origin/main`, deployed 125333db) | no | Vocabulary corrected throughout the content surface (panel, first column, description, empty state) with AR+EN regression proved by injection. Needs merge, deploy, and the owner seeing Ads and Content named apart — and written apart — in Production. | |
| 72 | P0 | A marketing path reported that nothing contributed to it while carrying 9,437.86 in spend and 566 orders | On the LIVE client report every path returned `coverage.state: no_contributors`, including the conversion path with real spend and orders | shared report; analytics objectives | AGGREGATION-TRUTH-001; CROSS-PLATFORM-ATTRIBUTION-DEPTH-001; CLIENT-REPORT-ENTITY-BOUNDARY-001 | AGGREGATION-TRUTH-001=**VERIFIED**<br>CROSS-PLATFORM-ATTRIBUTION-DEPTH-001=**PARTIAL** | Every path starts as `emptyPath()` with `no_contributors`, which is true of a path nothing has been added to. Rows are then accumulated into it — spend, orders, revenue, campaigns — and the coverage was never touched again. The inverse of the failure the state exists to prevent. | yes | yes | yes (125333db) | **yes** | — on the live client report the conversion path now reads `complete` with `["snapchat"]` against 9,437.86 spend and 566 orders, traffic likewise against 404.92, and awareness still reads `no_contributors` with nothing on it. No UUID appears in any coverage list, so the client boundary holds. | |
| 73 | P0 | The metric title and its value still sit on opposite edges — reported a SECOND time, from a screenshot, while every check passed | In Arabic the label sat at the right edge of its card and the figure at the left, a card's width apart. On the Content card, the KPI strip, the popup and the reports | /agency/content; /agency/analytics; dashboard; reports; campaigns; budgets | UX-KPI-PRESENTATION-001; TABLE-NUMERIC-ALIGNMENT-001 | UX-KPI-PRESENTATION-001=**PARTIAL**<br>TABLE-NUMERIC-ALIGNMENT-001=**PARTIAL** | `dir` does two things. It sets reading order, which «56.3K SAR» inside an Arabic page needs, and it re-bases every logical property on the element it sits on — so `text-align: start` inside a `dir="ltr"` box means LEFT. The first fix (KPI-ALIGNMENT-001) concluded the missing piece was `text-start` and added it to twenty-four surfaces, which is the same left alignment written out, and then `MetricStrip.test.tsx` asserted that pairing AS the rule. The defect had a test demanding it. The second miss was narrower: the new source guard recognised a block only when `block`, `grid` or `flex` appeared in the class list, and the content card's figure is a `<dd>` — a block because of what it IS — so the guard walked past the first surface the owner named | yes (#375) | yes (#375) | yes (`6dc6d4dd`, deploy run 34686483110) | no | **DEPLOYED 2026-09-12 AND NOT YET OWNER-ACCEPTED.** The shipped bundle carries the corrected code — «Video, no cover», «Everything that ran», «search by name to reach the rest» and the bidi isolate are all present in `assets/index-BnN0-bcO.js`, and the API answers healthy. What that does NOT establish is the owner-visible half: every one of these is a claim about what a person SEES on an authenticated Arabic page, and no authenticated Production session is available to this agent. Owner acceptance on the authenticated surfaces. Held in three browsers by `e2e/metric-alignment.spec.ts`, which measures TEXT RUNS rather than element boxes — three earlier drafts of that guard could not fail, one locating the figure by its isolation mechanism, one measuring an element box that fills its row whatever the alignment is, and one ranging over the element instead of its text nodes. The fourth fires at 72px on the pre-fix structure and at 72px on «الطلبات» on the content card | — |
| 74 | P0 | A cost printed as a percentage, and derived figures surviving the refusal of the spend they are made of | «CPM 65.65%» and «CPC 125.38%» in the popup opened from the Analytics content table, over a row reading «1.25 USD» about the same click; and «الإنفاق —» in that same panel beside both | /agency/analytics content table; creative popup | NUMBER-PRESENTATION-001; MONEY-USD-001 | NUMBER-PRESENTATION-001=**PARTIAL** | The dialog's figure list was inline in a three-thousand-line page and had drifted: CPC and CPM went through `rateOrDash`, which multiplies by a hundred and appends «%», while every table row in the SAME file used `rowCostPer`. ROAS printed «420.00%». Separately, spend went through the money contract — which had correctly refused a scope whose currencies cannot be added — and the costs were read off the row as plain numbers, so the refusal never reached the figures made of that spend | yes (#375) | yes (#375) | yes (`6dc6d4dd`, deploy run 34686483110) | no | **DEPLOYED 2026-09-12 AND NOT YET OWNER-ACCEPTED.** The shipped bundle carries the corrected code — «Video, no cover», «Everything that ran», «search by name to reach the rest» and the bidi isolate are all present in `assets/index-BnN0-bcO.js`, and the API answers healthy. What that does NOT establish is the owner-visible half: every one of these is a claim about what a person SEES on an authenticated Arabic page, and no authenticated Production session is available to this agent. Owner acceptance. The list is a module now, tested against the same readers the rows use, and `metricUnits` fails on any percentage reader holding a money metric's name — proved by injection on the exact production line | — |
| 75 | P0 | Content still shows no real visual preview, especially collection and multi-asset shapes, and the media area holds a paragraph where the picture belongs | A collection ad drew an empty frame while the payload said `available`; absent media was explained by a full sentence at 11px filling the 128-pixel poster box, repeated down the grid | /agency/content; creative popup; reports | CONTENT-PREVIEW-SHAPES-001; AD-PREVIEW-001 | CONTENT-PREVIEW-SHAPES-001=**IN_PROGRESS**<br>AD-PREVIEW-001=**PARTIAL** | `preview()` read only the creative's three own columns for the hero, and a carousel or collection frequently has none of them — the media is on the CARDS, in the JSON column the same class parses a few lines down. Every absence arm was guarded by `cards === null`, so an ad WITH cards and no hero matched none of them and fell to the default: state `available`, all three URLs null | yes (#375) | yes (#375) | yes (`6dc6d4dd`, deploy run 34686483110) | no | **DEPLOYED 2026-09-12 AND NOT YET OWNER-ACCEPTED.** The shipped bundle carries the corrected code — «Video, no cover», «Everything that ran», «search by name to reach the rest» and the bidi isolate are all present in `assets/index-BnN0-bcO.js`, and the API answers healthy. What that does NOT establish is the owner-visible half: every one of these is a claim about what a person SEES on an authenticated Arabic page, and no authenticated Production session is available to this agent. Owner acceptance against the real Snapchat collection. Recovery is proved as a unit — the first usable card becomes the hero, through the same credential guard, never for a withheld or expired ad, never over a hero of its own — but the E2E fixture holds no card-without-hero case, so this is NOT closed from fixtures | — |
| 76 | P0 | The giant full-screen viewer must not be the default thumbnail action; a thumbnail opens a concise popup, and the popup leads to the dedicated Content Analytics page | Clicking a thumbnail opened a full-screen page carrying a shallower copy of figures the creative's own page already draws, with no route from it to that page | /agency/content; creative popup; shared report | CONTENT-DETAIL-MODAL-001 | CONTENT-DETAIL-MODAL-001=**IMPLEMENTED_NOT_VERIFIED** | Three modals opened one ad and the heaviest was the default. `CreativeQuickFacts` drew identity, headline figures, a daily trend and the funnel; `CreativeDetailPage` draws identity, copy, figures, funnel, trend, per platform, peers, fatigue, evidence and insights. The reader met the smaller one and could not reach the larger. Separately the popup's only onward link was hardcoded `/app/content/…`, so every operator working in `/agency` was thrown across portals by it, and the ads table's popup had no link at all | yes (#375) | yes (#375) | yes (`6dc6d4dd`, deploy run 34686483110) | no | **DEPLOYED 2026-09-12 AND NOT YET OWNER-ACCEPTED.** The shipped bundle carries the corrected code — «Video, no cover», «Everything that ran», «search by name to reach the rest» and the bidi isolate are all present in `assets/index-BnN0-bcO.js`, and the API answers healthy. What that does NOT establish is the owner-visible half: every one of these is a claim about what a person SEES on an authenticated Arabic page, and no authenticated Production session is available to this agent. Owner acceptance. Held in three browsers by `e2e/owner-acceptance-content.spec.ts`: a thumbnail opens the popup, the popup offers the way onward, the link stays in the reader's portal, and following it reaches the creative's own page | — |
| 77 | P2 | Changing the interface language makes WebKit refuse the CSRF request, in development | Switching to English on any authenticated surface logs «XMLHttpRequest cannot load /sanctum/csrf-cookie due to access control checks» on WebKit; chromium and firefox accept the same request | every authenticated surface, development only | PORTAL-AUTH-001 | PORTAL-AUTH-001=**BLOCKED_OPERATIONAL_EVIDENCE** | Found by making `switchToEnglish` real: it had been a no-op that swallowed its own failure, so no test had ever changed the locale on a running page. Changing it re-renders, something then needs a CSRF token, and the proxied credentialed request is refused by WebKit alone. Proved by isolation — stubbing the helper to a no-op makes every affected case pass on WebKit. In development the SPA is on :5273 and the API on :8100 behind a Vite proxy; in PRODUCTION they share an origin, so the request is not cross-origin at all | no | no | n/a | no | Whether this reproduces on Production WebKit. It cannot be answered from here: the dev proxy is the suspect, and reproducing it needs a Safari session against the real origin. The specs that met it were asking for English out of habit — their assertions are language-agnostic — so the request was removed from them rather than the finding being hidden behind a helper that shrugs | Owner or operator with Safari on the live product |
| 78 | P0 | Content library top area is visually noisy — too many large KPI cards, clutter without enough value | Thirteen full-size KPI cards above the grid, nine of them reading «لا توجد بيانات» or «لم ترسله المنصة» on the owner's own account | /agency/content | UX-KPI-PRESENTATION-001; CONTENT-PREVIEW-SHAPES-001 | UX-KPI-PRESENTATION-001=**PARTIAL**<br>CONTENT-PREVIEW-SHAPES-001=**IN_PROGRESS** | Self-inflicted. CONTENT-KPI-TOTALS-001 correctly found the library had no headline figures at all, and answered it by naming every metric the catalogue holds — truthful and unreadable. Four figures now, plus the one comparison a dashboard cannot give: the split of spend across image, video and carousel. The share refuses exactly as the server's does — a format whose money could not be added is NAMED beside the bar rather than folded into its denominator, because a share over an incomplete total overstates every other slice by the amount nobody can see | yes (#376) | yes (#376) | yes (`31d38924`, deploy run 34694368315) | no | **DEPLOYED 2026-09-12 AND NOT YET OWNER-ACCEPTED.** Verified in the shipped bundle `assets/index-B1iR7m8U.js`. What that does NOT establish is the owner-visible half: this is a claim about what a person SEES on an authenticated page, and no authenticated Production session is available to this agent. Marker «Spend by content type» present in the bundle | — |
| 79 | P0 | The quick popup must lead with the creative and its chart, not with a stack of ids | The trend chart sat last, under eight metadata rows — off the screen entirely on a phone, in the panel somebody opened to judge a creative | creative popup on /agency/content and /agency/analytics | CONTENT-DETAIL-MODAL-001 | CONTENT-DETAIL-MODAL-001=**IMPLEMENTED_NOT_VERIFIED** | The panel rendered in the order the fields arrived in rather than the order a reader needs. Now hero → figures → chart → details (folded) → a CTA that is a button rather than a line of underlined text. The test asserts ORDER, because one that checked each piece was present would have passed the version the owner objected to | yes (#376) | yes (#376) | yes (`31d38924`, deploy run 34694368315) | no | **DEPLOYED 2026-09-12 AND NOT YET OWNER-ACCEPTED.** Verified in the shipped bundle `assets/index-B1iR7m8U.js`. What that does NOT establish is the owner-visible half: this is a claim about what a person SEES on an authenticated page, and no authenticated Production session is available to this agent. Held in three browsers by `e2e/owner-acceptance-content.spec.ts` | — |
| 80 | P1 | Recommendations must be a real optimization Action Center, not a list of written notes | An account where nobody had written a recommendation showed nothing to do — while the product knew a budget was breached, a creative had fatigued and a token was expiring | /agency/recommendations | ANALYTICS-DIAGNOSTIC-INTELLIGENCE-001; BUDGET-GOVERNANCE-001 | ANALYTICS-DIAGNOSTIC-INTELLIGENCE-001=**IN_PROGRESS**<br>BUDGET-GOVERNANCE-001=**PARTIAL** | Three engines already knew — `AlertEvaluator`, `SpendLimitGovernor`, `CreativeFatigue` — and each had its own surface; the page named «what should I do» read none of them. It derives no advice: it joins and ORDERS what they said, and every item keeps its SOURCE, because «fatigue says this creative is spent» and «Sara wrote: stop running this» are different claims with different standing | yes (#376) | yes (#376) | yes (`31d38924`, deploy run 34694368315) | no | **DEPLOYED 2026-09-12 AND NOT YET OWNER-ACCEPTED.** Verified in the shipped bundle `assets/index-B1iR7m8U.js`. What that does NOT establish is the owner-visible half: this is a claim about what a person SEES on an authenticated page, and no authenticated Production session is available to this agent. Marker «not recommendations anybody wrote» present. Owner acceptance needs an account with live signals, which the demo data does not raise | — |
| 81 | P1 | Alerts need a real monitoring taxonomy — severity alone does not say what kind of problem it is | An expiring token and a campaign spending with no results are both «warning», go to different people and lead to opposite actions, with no way to tell them apart but reading every message | /agency/alerts | EMAIL-SETTINGS-DEPTH-001 | EMAIL-SETTINGS-DEPTH-001=**IN_PROGRESS** | Four kinds read from `AlertEvaluator`'s own list rather than a second taxonomy, a next action per TYPE — the same every time, whatever the figures were — and a category filter offered only where more than one kind is present. Every type the product can raise is asserted to have both, checked against `ALERT_TYPES`. `sla_warning` has neither and that is asserted rather than filled in: the backend names it in `UNRAISED` because nothing raises it | yes (#376) | yes (#376) | yes (`31d38924`, deploy run 34694368315) | no | **DEPLOYED 2026-09-12 AND NOT YET OWNER-ACCEPTED.** Verified in the shipped bundle `assets/index-B1iR7m8U.js`. What that does NOT establish is the owner-visible half: this is a claim about what a person SEES on an authenticated page, and no authenticated Production session is available to this agent. Markers «Data and integrations» and «Reconnect the account» present. No alert has fired in the demo account, so the rendered row is held by a component test rather than a browser one — an E2E was written and deleted because the evaluator raises on a schedule and the spec passed through its own empty-state branch | — |
| 82 | P0 | Detailed Report creative previews are broken — rows say «لا يوجد غلاف» for creatives whose media Content shows | Opened the real Detailed Report `/r/5mte9VfXx32FB8VmQIrTzm` on production: previews missing across the creative sections, and roster rows claiming no cover for ads the Content library displays perfectly. A client reads this document. | Shared report link, the PDF, the operator's own report view | REPORT-AD-PREVIEW-001; CONTENT-PREVIEW-SHAPES-001 | IMPLEMENTED_NOT_VERIFIED | `CreativeRows::lean()` builds every roster row and never sets `preview` — deliberately, because a stored snapshot outlives a signed media URL and must not carry a credential into a client's document. The renderer draws `AdPoster preview={row.preview ?? null}` and `RosterRow.preview` is OPTIONAL, so the absent field type-checked, the poster was always handed null, and the absence state fired for every creative whatever its media. Measured on the owner's own report before the fix: `ads_roster` 1,539 rows with zero preview envelopes, beside `ads` 5/5 and `ads_groups` 9/9 all `available` — the roster was the whole defect on that document, and it is a LIVE share rather than a snapshot. | `ReportCreativeMedia` resolves the media when the report is READ, through the same `CreativePresenter` the Content library uses — figures stay the snapshot's, pictures are current. Wired into all four read paths (shared link, live link, PDF, operator view), before the client boundary strips the ids it keys on. | PR #379 (snapshot/PDF/operator paths) + PR #382 (the live path) | #379 merged 3f7ae690 | #379 deployed 2026-09-12 | **YES — 2026-09-12, on the owner's own link.** The roster went from 1,539 rows with zero preview envelopes to zero rows without one: `available` 1,521, `expired` 13, `shape_not_fetched` 4, `unavailable` 1, with 1,534 carrying real media. #379 fixed the snapshot, PDF and operator paths and did NOT fix this report, which is a live share; #382 attached the media inside `LiveReportService::build()`, where the creative ids still exist. Cost measured at the same time: 1.6-1.9s/444KB before, 3.1s/1.02MB after. | none |
| 83 | P0 | The quick popup is cramped — the creative, the KPIs and the chart cannot be seen without scrolling | `max-w-2xl`, 672 pixels of a 1440-pixel screen, one column. Reverting to it fails the new guard with the complaint as a number: the KPI row ends 903px down a 900px viewport. | Content library quick popup | CONTENT-DETAIL-MODAL-001 | IMPLEMENTED_NOT_VERIFIED | One column cannot satisfy «creative visually dominant» and «chart in the first viewport» at any width — a taller hero pushes the chart further down. | Two columns above `lg` at 1024 of 1440: media one side, the reading of it the other. Short of the screen on purpose, so it stays a panel over the library. Phone unchanged. | PR #380 | pending | pending | Owner’s own account after deploy | none |
| 84 | P0 | The popup's trend chart said «تعذّر تحميل الاتجاه الزمني» for a creative that is in the library | The panel requested the clicked creative and received 404 «العنصر المطلوب غير موجود». Found by reading the panel's network traffic; every unit test mocks that call. | Content library quick popup | CONTENT-DETAIL-MODAL-001 | IMPLEMENTED_NOT_VERIFIED | `CreativeTrend` asked the project-scoped `/projects/{project}/creatives/{creative}`. The library spans projects and a card carries no project id — `CreativeAnalysisController::detail` says so in its own docblock, and `CreativeDetailPage` has always used the membership-scoped endpoint. The component written to stop the modal and the page deriving a trend two ways had drifted from the page at the endpoint. | One endpoint: `getCreativeInReach`, the detail page's. Now 200 and the chart draws. | PR #380 | pending | pending | Owner’s own account after deploy | none |
| 85 | P0 | Spend is still missing from content creatives/cards on Production | The card renders `headline_metrics.slice(0, 4)`, and `headline_metrics` is the server's answer to «what is this creative JUDGED on» — an objective question. Spend is in that list for some objectives and not others, so it vanished from the card for every creative whose objective did not headline it, beside a table that renders Spend explicitly. | Content Library grid card; Content Analytics platform table; Content GROUPS card and its per-platform table (found 2026-09-13 by sweeping the surfaces the owner named) | CONTENT-SPEND-ALWAYS-001; CONTENT-KPI-COVERAGE-002 | IMPLEMENTED_NOT_VERIFIED | The money reader handled it correctly the whole time — nobody asked it. `contentVisible.test.tsx` asserts Spend appears and passes, because its fixture is `headline_metrics: ['spend', 'impressions']`: a fixture containing the thing under test is how a defect survives its own regression test. | Spend is a FIXED first cell read through `creativeMoney`, the canonical reader the table, popup, Content Analytics and report share; the objective's own metrics follow it, capped at three so the card stays compact. «Cannot headline it» no longer withholds the price. `CreativeDetailPage`'s platform table read spend through `metricState` (converted column only), so the same creative said «412.50 USD» in the library and «No data» one click later — it reads `creativeMoney` now too. **THE GROUPS SURFACE WAS THE THIRD RENDERER OF THE SAME FIGURES AND THE CARD'S FIX DID NOT REACH IT.** `CreativeGroupsPage` had both halves: `headline_metrics.length > 0 ? headline_metrics : ['spend', …]` guaranteed the figure only in the case where the product knows LEAST about the group, so an awareness group dropped spend while a mixed-objective group kept it; and every cell went through `metricState`, so a withheld Snapchat amount read «No data» beside a library card saying «412.50 USD» for the same creative. Fixed in both places it renders figures, the same way the library was. tsc then found WHY this class keeps recurring: `CreativeMetrics` never declared the six money-truth fields the backend emits, so every money call site cast past the type with `as never` — and a cast is not a contract. Declared; all four casts removed; the money path is now type-checked. | PR #389 | Production: deployed bundle carries the fixed-cell path verbatim; the live Detailed Report `5mte9VfXx32FB8VmQIrTzm` renders 50 roster rows each stating Snapchat spend in original USD, 0 NaN / 0 undefined / 0 false absence | 3-browser owner-acceptance chain over library, groups and the Analytics content table; `groupSpendAlways.test.tsx` 12 cases in both locales; injection fails 6 and 2 independently | Authenticated Production Content Library, especially Snapchat originals | none |
| 86 | P0 | The creative in the quick popup is cropped and the panel is too small to judge the ad | Measured in a browser on the real panel, not inferred: computed `object-fit` was `cover`, and a 600×600 creative was drawn into a 497×416 box, losing about 16% of its height off the top and bottom. In an 8-card sample of the demo library 3 cards rendered an image, 3 rendered a film and 2 carried no asset, so the shape rule reached only a third of the surface it was written for. | Content library quick popup | CONTENT-DETAIL-MODAL-001; CONTENT-PREVIEW-SHAPES-001 | IMPLEMENTED_NOT_VERIFIED | `AdPoster` injected `object-cover` for every preview it did not read as portrait, and the dialog passed `object-contain` to the same element; both utilities landed and the browser's cascade chose `cover`. `previewShape` calls anything under `height > width * 1.2` landscape, so 1:1 and 16:9 — most of a real library — were the shapes being cut, while 9:16 escaped. The film had no stage ceiling at all: `max-h-[70vh]` is correct on a page where the player is the tallest thing present and spills a 58vh stage whose overflow is hidden. | Both media paths now take their fit from the caller, and a centred stage bounds the asset on both axes, so no ratio can crop and none can overflow — including an asset whose dimensions the platform never reported, which is the case a rule per format mishandles silently. The stage keeps its height in `vh`, so hero, figures, trend and the way on stay inside a 900-pixel viewport. Acceptance asserts the measurement rather than the class list, because the broken markup contained the correct class all along. | e6ab2df7 (#397) | yes — served bundle `index-Cs4yIQuc.js` carries the stage, the 88vw shell and the contained fit | no | The code is live and measured green on three engines, but the owner asked for REAL creatives, and synthetic rulers are not that: image, video, Story/Snap vertical, square, landscape, carousel and collection each seen whole, with compact KPIs, the chart visible and no popup scrolling. That needs an authenticated Production session, which this lane does not have — BLOCKED_OPERATIONAL_EVIDENCE for the visual sweep, not for the fix. | none |
| 87 | P0 | PDF export does not work — the owner clicks it in Production and no file arrives | Reproduced from code and deployment config rather than guessed. `deploy/backend.production.env.example` sets `REPORTS_CHROMIUM_ENABLED=false`, and `deploy/backend.production.Dockerfile` (php:8.4-fpm-alpine) installs no Node, no browser and no python3/pikepdf. The renderer needs all three: it spawns Node on `scripts/report-print.mjs`, resolves a browser through Playwright, then runs a FAIL-CLOSED Arabic text-layer pass on every PDF. CI is green because its runners install each one and the gate sets the flag for itself. | Reports table export action | REPORT-EXPORT-FUNCTIONAL-001 | IMPLEMENTED_NOT_VERIFIED | With the flag off a client or executive PDF throws by design — the product refuses to ship a Dompdf fallback to a client — and an internal one produces a Dompdf file that `ReportDownloadController` then refuses on a renderer-version mismatch. Either way no download event ever fires, which is exactly what the owner sees. | One unit, because it is one feature: the failure is now truthful AND the runtime is provisioned. The image installs Alpine's chromium (Playwright's builds are glibc, the base is musl, so the supported path is an explicit `REPORTS_CHROMIUM_PATH`), `playwright-core` at a path a server actually has, `font-noto-arabic` so Arabic is not tofu, and python3 + pikepdf without which no PDF is downloadable at all. The env template enables the renderer and names it honestly; the checklist stops claiming the server needs no Node. A new CI `image` job builds that image and asks it for every binary — the host rebuilds from main on merge, and CI was the one place that never looked. | 7c8479d1 (#398) | yes — deploy succeeded and the export states, failure codes and both languages of the copy are in the served bundle `index-CpCbjFd7.js` | no | The renderer runtime is provisioned and CI builds the production image and asks it for node, chromium, python3, pikepdf, an Arabic face and both renderer scripts — but that proves the IMAGE, not the running export. What is owed is the artefact: a PDF exported from the owner's own Production account, downloaded, opened, and read for data, media, RTL and branding. That needs an authenticated Production session this lane does not have. BLOCKED_OPERATIONAL_EVIDENCE — and a green gate is explicitly not a substitute, because CI installs what Production did not. | BLOCKED_OPERATIONAL_EVIDENCE — authenticated Production artifact download required. Every unauthenticated path was tried first and none reaches a real artefact: the project publishes no sample `/r/:token`, the documented `/l/jm4bf2p` belongs to SHORT-LINKS-001 and is not a report share, and the diagnostics workflow runs a fixed read-only command by design. Guessing a token would be enumeration against a client surface, so it was not attempted. |
| 88 | P0 | Several report settings and actions look cosmetic — clicking export appears to do nothing at all | The reports table asked only for exports whose status was `completed`. A render still in flight and a render that had FAILED both fell through to the same «PDF» button, and the reason sitting in the export row reached nobody. The list also stopped watching: it polled only while a REPORT was processing, plus one refresh 1.5s after the click, which is shorter than a browser render — so even an export that SUCCEEDED stayed invisible until somebody reloaded the page. | Reports table export action | REPORT-EXPORT-FUNCTIONAL-001 | IMPLEMENTED_NOT_VERIFIED | Two independent faults of one shape: the interface had no state for «running» and none for «failed», and the payload carried no reason to show even if it had. | A code per failure (`ExportFailureReason`) the interface turns into an actionable sentence in Arabic and English; a running state that does not blame the renderer for a file still being made; a failed state that retries and says why; the newest attempt describing a format so a successful retry stops wearing the old error; and the list polling while any export is in flight. The renderer message never crosses the wire — it carries stderr, binaries and absolute paths. | 7c8479d1 (#398) | yes — deploy succeeded and the export states, failure codes and both languages of the copy are in the served bundle `index-CpCbjFd7.js` | no | The states are live in the bundle. What is owed is the owner seeing one: an export that fails and says why in words, and an export that succeeds appearing without a manual refresh, on the real account. Needs an authenticated Production session. | BLOCKED_OPERATIONAL_EVIDENCE — authenticated Production artifact download required. Every unauthenticated path was tried first and none reaches a real artefact: the project publishes no sample `/r/:token`, the documented `/l/jm4bf2p` belongs to SHORT-LINKS-001 and is not a report share, and the diagnostics workflow runs a fixed read-only command by design. Guessing a token would be enumeration against a client surface, so it was not attempted. |
| 89 | P0 | The live shared report must be a concise multi-platform executive dashboard, not a document — every selected platform in one view, and settings that really alter the report | The comparison a client opens the report to read did not exist: the page drew spend over time and a share-of-spend donut, and neither answers «which platform did better». The one platform table the product had lived in the detailed form, carried no cost per result and no rate, and a link selecting six platforms published four rows — the four with data, with nothing to distinguish a platform that reported nothing from one the report does not cover | `/reports/share/:token` (live) | LIVE-CROSS-PLATFORM-001; REPORT-PRODUCT-MODEL-001; CLIENT-FACING-PRESENTATION-001 | IMPLEMENTED_NOT_VERIFIED | Two separate causes. The table was gated to the detailed form, which is the DEFAULT form, so the first attempt to promote it rendered for nobody — found by opening the page, not by reading the condition. And the rows came from `payload.platforms`, which the aggregator fills only with platforms that reported, so the section silently answered «the platforms that had data» | One platform table, carried by both live forms, with the two columns that let a reader rank by price instead of volume. `cpa` and `ctr` are the SERVER's — recomputing them in the browser was a second arithmetic for one figure and skipped the money contract, printing a withheld spend as free and a zero-result platform as «Infinity». Every selected platform keeps a row, and one that reported nothing says so in words rather than in six dashes. Both cost-per columns print the exact figure: `money()` rounds 31.5 to «32 SAR», and the KPI cards on the same page had already made that call. Section flags decide whether it appears at all — 7f1c9fa7 (#408) | yes — merged and deployed, main run green | yes — the served bundle `index-Dh0amz30.js` carries `live-platform-comparison`, «مقارنة أداء المنصات» / «Platform performance», the «لم تُبلِّغ عن بيانات» / «reported nothing» marker and the «تكلفة النتيجة» column. Checked in the same fetch: the approved Arabic name «كامبينز هب» is present, the fatha spelling and the legacy `#863bff` are absent, so #407's brand contract holds in production | no | The bundle proves the section shipped; it does not prove what a CLIENT sees. What is owed is the owner opening a real multi-platform link and reading the table — every selected platform present, the silent ones saying so, and the cost per result matching the KPI card above it. This lane has no authenticated Production session and the project publishes no sample `/r/:token`; guessing a token would be enumeration against a client surface | BLOCKED_OPERATIONAL_EVIDENCE — authenticated Production session required |
| 90 | P0 | The detailed report must give platform-by-platform depth — a summary per platform, its own best creatives with real previews, and every promoted creative reachable | Not yet built. The detailed form today adds one objective table below the dashboard | `/reports/share/:token` (detailed) | REPORT-DETAIL-PARITY-001; REPORT-AD-PREVIEW-001 | IMPLEMENTED_NOT_VERIFIED | The report ended in ONE gallery grouped by objective across every platform, and the deck's per-platform slide filtered the scope-wide ranking to that provider — «which of the report's best ads happen to run here», empty for any platform that did not place in the overall cut | Per-platform summaries, per-platform best creatives ranked by what the objective is judged on, previews through the canonical resolver rather than a report-only one, and the Top-3 named rather than silent — ac8c07de (#409) | yes — merged and deployed, main run green | yes — the served bundle `index-CtRDQBVO.js` carries `report-platform-creatives`, `report-platform-summary`, «الأعلى أداءً في كل منصة» / «What performed best, platform by platform», the «creative(s) in this window» count and the «the rest are in the creative roster below» disclosure | no | The bundle proves the section shipped. What is owed is the owner opening a real multi-platform DETAILED link and seeing each platform's own leaders with real previews — the local demo's ads are `never_fetched`, which is the honest empty state and not a resolver fault, so previews can only be judged on an account whose creatives were actually fetched | BLOCKED_OPERATIONAL_EVIDENCE — authenticated Production session required |
| 91 | P0 | SECURITY — a member confined to one client could pause, archive, rename or clone ANOTHER client's project by putting its id in the URL | Found by checking TEAM-PROJECT-RBAC-001's own acceptance rather than re-reading its note. `show` asks `reachable()` before answering, because a confined viewer could read a neighbouring client's record that way. `update`, `archive`, `restore`, `pause`, `resume` and `clone` asked nothing of the kind: the TENANT permission, then a tenant-scoped `find()` that finds every project in the agency. Reproduced as three failing requests before any fix | `/api/v1/projects/{project}` lifecycle routes | TEAM-PROJECT-RBAC-001 | IMPLEMENTED_NOT_VERIFIED | The narrowing was added where the lesson was learned — on the READ — and the writes beside it were left checking «may this person run clients at all», which is not a scope. `projects.update` names no client. The lifecycle routes sit outside `ResolveProject`, which has applied this narrowing to every project-scoped route since it was written, so their protection was a line in a controller somebody had to remember | One shared `authorizeReach()` on all seven lifecycle routes, and a sweep that finds them by what they LACK — a `{project}` in the path and no `ResolveProject` above it — driving each as a confined member and requiring 403, never a 2xx. Two controls hold it to a narrowing rather than a lockout: the same member still acts on their own project, and an agency-wide `projects.view.all` holder is not narrowed. Removing the guard makes the sweep name all seven including `show`, so it would have caught the original defect. Backend suite green at 3,574 with the tightening in place | yes — 94a92243 (#410), main run green | yes — deployed. Checked from outside: `/api/v1/health` answers «CampaignsHub API» and an unauthenticated POST to a lifecycle route answers 401 — the route is reachable and guarded at the session layer | no | The 401 proves the route and the session gate, NOT the narrowing this row is about: that needs two users in one tenant with different memberships, one acting on the other's project, which needs an authenticated Production session this lane does not have. The refusal itself is proved by request in CI, on every one of the seven routes | BLOCKED_OPERATIONAL_EVIDENCE — authenticated Production session with two memberships required |
| 92 | P0 | Clicking the CampaignsHub identity must go to the canonical dashboard home when signed in, and to the marketing `/` on the public site — and AR/EN identity and browser titles must follow the language | The desktop rail rendered the lockup as a plain `<div>`: clicking it did nothing, on the surface operators use most. The public header already linked to `/`. `PublicPageShell` interpolated the locale into the title inside an effect that depended on `[title]` alone, so switching language never retitled the tab; the marketing homepage set no title at all | authenticated rail/collapsed rail/drawer/mobile; public header; browser titles | BRAND-MARK-001 | IMPLEMENTED_NOT_VERIFIED | A dead logo raises no error — it simply fails to respond, so nobody reports it. The title effect read as though it handled the locale, because it called `productName(locale)`; what was wrong was WHEN it ran | One `Brand` link serves the rail, the collapsed rail and the drawer, so the four surfaces are one change; only the MARK links in the agency and influencer rails, because the words beside it are the tenant's name. A guard written for the rail found the influencer phone bar pointing at `/influencer`, a path the router does not have. Titles now depend on the locale they interpolate, and the homepage names itself in the language it is read in — this PR | pending | pending | no | Owner acceptance across AR+EN × light+dark × desktop+mobile on the deployed site | — |
| 93 | P0 | The marketing homepage visibly shifts on mobile immediately after opening, then settles | Reproduced from the served bytes, not inferred: `curl` shows `<html lang="en">` with no `dir` and no `data-theme`, while the app resolves to Arabic, RTL and dark when nothing is remembered. Every first visit paints LTR and light, then flips when `applyDocument()` runs — header sides, hero layout and document colour. Measured CLS on production at 390px came back 0.0000 on a WARM reload, which is exactly the warm-cache concealment the owner warned about | `campaignshub.io/` on a phone (390px, 393px) | MKT-FIX-001 | PARTIAL | The document's starting state was never the app's resolved state. `index.html` shipped one answer and the store computed another, and the gap between them is the first paint | Correct static defaults plus a blocking pre-paint script that applies the remembered preference before the stylesheet paints — no delay, no opacity, no splash. A unit guard holds the document's defaults to the store's own. The first E2E written for this could not fail: Playwright's auto-waiting `toHaveAttribute` waits for the attribute to BECOME correct, so it passed with the fix deleted; rewritten to read the served bytes and to load with the bundle blocked, it fails four of ten without the fix — this PR | pending | pending | no | A real cold load on the deployed site at 390–393px, and CLS evidence from a cold cache | — |
| 94 | P0 | Content metrics not working reliably on Production — Spend missing or unavailable on promoted creatives, other creative indicators missing or inconsistent. **OPEN. The owner still observes this on Production, which overrides any reading taken from a diagnosis command.** Split into its real clauses below (94a–94h) so no detail disappears under the word «Content» | Traced provider → ingestion → grain → aggregation → presenter → card before touching any React. `AccountMetricsSyncer` fetches creative-level insights behind `instanceof ReportsCreativeInsights` and Snapchat is the ONLY implementor, so Meta, Google, TikTok, LinkedIn and X never write a `creative_daily_metrics` row — the one table the content library reads. The failure is silent by design: the call is wrapped in a swallow because «creative numbers are an enrichment» | `/app/content` cards, popup, Content Analytics | CONTENT-SPEND-ALWAYS-001; CONTENT-KPI-COVERAGE-002 | IMPLEMENTED_NOT_VERIFIED | Rung 2/3, not rendering. The same `instanceof` shape this codebase already fixed one rung up, where its own note says «the product printed our silence as the platform's». Meta implements `ReportsEntityGrains`, so the spend has been sitting in `entity_daily_metrics` keyed by the ad that ran the creative, underneath a card reading «—» | A creative with no rows of its own reports what its ADS reported — never blended with a native creative grain, summed across ads that share it, nothing attributed from an ad with no creative, and a metric the ad grain has no column for left ABSENT rather than turned into a fabricated zero. Separately, both creative tables carried an unread `is_demo` column while `MetricsAggregator` had guarded the equivalent against «invented money inside a real total»; the same derived policy now applies — this PR | yes — 9638b85c (#414) | yes — the main run completed against exactly this commit (`9638b85c`) and the API answers | no | **There is nothing to grep and nothing to see.** The fix is backend-only, so no served bundle carries a string that proves it, and the figures it restores cannot appear without a real provider account: every credential on this install is a seeded placeholder. What is owed is the owner opening `/app/content` on a connected Meta or Google account and reading a spend that is not «—», then reconciling it against the platform's own reporting for the same window | BLOCKED_OPERATIONAL_EVIDENCE — a connected real account is the only thing that can show this |
| 94a | P0 | Content — SPEND on a promoted creative |  Traced provider → ingestion → grain → aggregation → presenter → card before touching any React. `AccountMetricsSyncer` fetches creative-level insights behind `instanceof ReportsCreativeInsights` and Snapchat is the ONLY implementor, so Meta, Google, TikTok, LinkedIn and X never write a `creative_daily_metrics` row — the one table the content library reads. The failure is silent by design: the call is wrapped in a swallow because «creative numbers are an enrichment» | `/app/content` cards, popup, Content Analytics | CONTENT-SPEND-ALWAYS-001; CONTENT-KPI-COVERAGE-002 | IMPLEMENTED_NOT_VERIFIED | Rung 2/3, not rendering. The same `instanceof` shape this codebase already fixed one rung up, where its own note says «the product printed our silence as the platform's». Meta implements `ReportsEntityGrains`, so the spend has been sitting in `entity_daily_metrics` keyed by the ad that ran the creative, underneath a card reading «—» | A creative with no rows of its own reports what its ADS reported — never blended with a native creative grain, summed across ads that share it, nothing attributed from an ad with no creative, and a metric the ad grain has no column for left ABSENT rather than turned into a fabricated zero. Separately, both creative tables carried an unread `is_demo` column while `MetricsAggregator` had guarded the equivalent against «invented money inside a real total»; the same derived policy now applies — this PR | yes — 9638b85c (#414) | yes — the main run completed against exactly this commit (`9638b85c`) and the API answers | no | **There is nothing to grep and nothing to see.** The fix is backend-only, so no served bundle carries a string that proves it, and the figures it restores cannot appear without a real provider account: every credential on this install is a seeded placeholder. What is owed is the owner opening `/app/content` on a connected Meta or Google account and reading a spend that is not «—», then reconciling it against the platform's own reporting for the same window | BLOCKED_OPERATIONAL_EVIDENCE — a connected real account is the only thing that can show this |
| 94b | P0 | Content — RESULTS on a promoted creative |  Traced provider → ingestion → grain → aggregation → presenter → card before touching any React. `AccountMetricsSyncer` fetches creative-level insights behind `instanceof ReportsCreativeInsights` and Snapchat is the ONLY implementor, so Meta, Google, TikTok, LinkedIn and X never write a `creative_daily_metrics` row — the one table the content library reads. The failure is silent by design: the call is wrapped in a swallow because «creative numbers are an enrichment» | `/app/content` cards, popup, Content Analytics | CONTENT-SPEND-ALWAYS-001; CONTENT-KPI-COVERAGE-002 | IMPLEMENTED_NOT_VERIFIED | Rung 2/3, not rendering. The same `instanceof` shape this codebase already fixed one rung up, where its own note says «the product printed our silence as the platform's». Meta implements `ReportsEntityGrains`, so the spend has been sitting in `entity_daily_metrics` keyed by the ad that ran the creative, underneath a card reading «—» | A creative with no rows of its own reports what its ADS reported — never blended with a native creative grain, summed across ads that share it, nothing attributed from an ad with no creative, and a metric the ad grain has no column for left ABSENT rather than turned into a fabricated zero. Separately, both creative tables carried an unread `is_demo` column while `MetricsAggregator` had guarded the equivalent against «invented money inside a real total»; the same derived policy now applies — this PR | yes — 9638b85c (#414) | yes — the main run completed against exactly this commit (`9638b85c`) and the API answers | no | **There is nothing to grep and nothing to see.** The fix is backend-only, so no served bundle carries a string that proves it, and the figures it restores cannot appear without a real provider account: every credential on this install is a seeded placeholder. What is owed is the owner opening `/app/content` on a connected Meta or Google account and reading a spend that is not «—», then reconciling it against the platform's own reporting for the same window | BLOCKED_OPERATIONAL_EVIDENCE — a connected real account is the only thing that can show this |
| 94c | P0 | Content — COST PER RESULT |  Traced provider → ingestion → grain → aggregation → presenter → card before touching any React. `AccountMetricsSyncer` fetches creative-level insights behind `instanceof ReportsCreativeInsights` and Snapchat is the ONLY implementor, so Meta, Google, TikTok, LinkedIn and X never write a `creative_daily_metrics` row — the one table the content library reads. The failure is silent by design: the call is wrapped in a swallow because «creative numbers are an enrichment» | `/app/content` cards, popup, Content Analytics | CONTENT-SPEND-ALWAYS-001; CONTENT-KPI-COVERAGE-002 | IMPLEMENTED_NOT_VERIFIED | Rung 2/3, not rendering. The same `instanceof` shape this codebase already fixed one rung up, where its own note says «the product printed our silence as the platform's». Meta implements `ReportsEntityGrains`, so the spend has been sitting in `entity_daily_metrics` keyed by the ad that ran the creative, underneath a card reading «—» | A creative with no rows of its own reports what its ADS reported — never blended with a native creative grain, summed across ads that share it, nothing attributed from an ad with no creative, and a metric the ad grain has no column for left ABSENT rather than turned into a fabricated zero. Separately, both creative tables carried an unread `is_demo` column while `MetricsAggregator` had guarded the equivalent against «invented money inside a real total»; the same derived policy now applies — this PR | yes — 9638b85c (#414) | yes — the main run completed against exactly this commit (`9638b85c`) and the API answers | no | **There is nothing to grep and nothing to see.** The fix is backend-only, so no served bundle carries a string that proves it, and the figures it restores cannot appear without a real provider account: every credential on this install is a seeded placeholder. What is owed is the owner opening `/app/content` on a connected Meta or Google account and reading a spend that is not «—», then reconciling it against the platform's own reporting for the same window | BLOCKED_OPERATIONAL_EVIDENCE — a connected real account is the only thing that can show this |
| 94d | P0 | Content — objective-aware KPI selection |  Traced provider → ingestion → grain → aggregation → presenter → card before touching any React. `AccountMetricsSyncer` fetches creative-level insights behind `instanceof ReportsCreativeInsights` and Snapchat is the ONLY implementor, so Meta, Google, TikTok, LinkedIn and X never write a `creative_daily_metrics` row — the one table the content library reads. The failure is silent by design: the call is wrapped in a swallow because «creative numbers are an enrichment» | `/app/content` cards, popup, Content Analytics | CONTENT-SPEND-ALWAYS-001; CONTENT-KPI-COVERAGE-002 | IMPLEMENTED_NOT_VERIFIED | Rung 2/3, not rendering. The same `instanceof` shape this codebase already fixed one rung up, where its own note says «the product printed our silence as the platform's». Meta implements `ReportsEntityGrains`, so the spend has been sitting in `entity_daily_metrics` keyed by the ad that ran the creative, underneath a card reading «—» | A creative with no rows of its own reports what its ADS reported — never blended with a native creative grain, summed across ads that share it, nothing attributed from an ad with no creative, and a metric the ad grain has no column for left ABSENT rather than turned into a fabricated zero. Separately, both creative tables carried an unread `is_demo` column while `MetricsAggregator` had guarded the equivalent against «invented money inside a real total»; the same derived policy now applies — this PR | yes — 9638b85c (#414) | yes — the main run completed against exactly this commit (`9638b85c`) and the API answers | no | **There is nothing to grep and nothing to see.** The fix is backend-only, so no served bundle carries a string that proves it, and the figures it restores cannot appear without a real provider account: every credential on this install is a seeded placeholder. What is owed is the owner opening `/app/content` on a connected Meta or Google account and reading a spend that is not «—», then reconciling it against the platform's own reporting for the same window | BLOCKED_OPERATIONAL_EVIDENCE — a connected real account is the only thing that can show this |
| 94e | P0 | Content — media and preview truth |  Traced provider → ingestion → grain → aggregation → presenter → card before touching any React. `AccountMetricsSyncer` fetches creative-level insights behind `instanceof ReportsCreativeInsights` and Snapchat is the ONLY implementor, so Meta, Google, TikTok, LinkedIn and X never write a `creative_daily_metrics` row — the one table the content library reads. The failure is silent by design: the call is wrapped in a swallow because «creative numbers are an enrichment» | `/app/content` cards, popup, Content Analytics | CONTENT-SPEND-ALWAYS-001; CONTENT-KPI-COVERAGE-002 | IMPLEMENTED_NOT_VERIFIED | Rung 2/3, not rendering. The same `instanceof` shape this codebase already fixed one rung up, where its own note says «the product printed our silence as the platform's». Meta implements `ReportsEntityGrains`, so the spend has been sitting in `entity_daily_metrics` keyed by the ad that ran the creative, underneath a card reading «—» | A creative with no rows of its own reports what its ADS reported — never blended with a native creative grain, summed across ads that share it, nothing attributed from an ad with no creative, and a metric the ad grain has no column for left ABSENT rather than turned into a fabricated zero. Separately, both creative tables carried an unread `is_demo` column while `MetricsAggregator` had guarded the equivalent against «invented money inside a real total»; the same derived policy now applies — this PR | yes — 9638b85c (#414) | yes — the main run completed against exactly this commit (`9638b85c`) and the API answers | no | **There is nothing to grep and nothing to see.** The fix is backend-only, so no served bundle carries a string that proves it, and the figures it restores cannot appear without a real provider account: every credential on this install is a seeded placeholder. What is owed is the owner opening `/app/content` on a connected Meta or Google account and reading a spend that is not «—», then reconciling it against the platform's own reporting for the same window | BLOCKED_OPERATIONAL_EVIDENCE — a connected real account is the only thing that can show this |
| 94f | P0 | Content — the review popup |  Traced provider → ingestion → grain → aggregation → presenter → card before touching any React. `AccountMetricsSyncer` fetches creative-level insights behind `instanceof ReportsCreativeInsights` and Snapchat is the ONLY implementor, so Meta, Google, TikTok, LinkedIn and X never write a `creative_daily_metrics` row — the one table the content library reads. The failure is silent by design: the call is wrapped in a swallow because «creative numbers are an enrichment» | `/app/content` cards, popup, Content Analytics | CONTENT-SPEND-ALWAYS-001; CONTENT-KPI-COVERAGE-002 | IMPLEMENTED_NOT_VERIFIED | Rung 2/3, not rendering. The same `instanceof` shape this codebase already fixed one rung up, where its own note says «the product printed our silence as the platform's». Meta implements `ReportsEntityGrains`, so the spend has been sitting in `entity_daily_metrics` keyed by the ad that ran the creative, underneath a card reading «—» | A creative with no rows of its own reports what its ADS reported — never blended with a native creative grain, summed across ads that share it, nothing attributed from an ad with no creative, and a metric the ad grain has no column for left ABSENT rather than turned into a fabricated zero. Separately, both creative tables carried an unread `is_demo` column while `MetricsAggregator` had guarded the equivalent against «invented money inside a real total»; the same derived policy now applies — this PR | yes — 9638b85c (#414) | yes — the main run completed against exactly this commit (`9638b85c`) and the API answers | no | **There is nothing to grep and nothing to see.** The fix is backend-only, so no served bundle carries a string that proves it, and the figures it restores cannot appear without a real provider account: every credential on this install is a seeded placeholder. What is owed is the owner opening `/app/content` on a connected Meta or Google account and reading a spend that is not «—», then reconciling it against the platform's own reporting for the same window | BLOCKED_OPERATIONAL_EVIDENCE — a connected real account is the only thing that can show this |
| 94g | P0 | Content Analytics |  Traced provider → ingestion → grain → aggregation → presenter → card before touching any React. `AccountMetricsSyncer` fetches creative-level insights behind `instanceof ReportsCreativeInsights` and Snapchat is the ONLY implementor, so Meta, Google, TikTok, LinkedIn and X never write a `creative_daily_metrics` row — the one table the content library reads. The failure is silent by design: the call is wrapped in a swallow because «creative numbers are an enrichment» | `/app/content` cards, popup, Content Analytics | CONTENT-SPEND-ALWAYS-001; CONTENT-KPI-COVERAGE-002 | IMPLEMENTED_NOT_VERIFIED | Rung 2/3, not rendering. The same `instanceof` shape this codebase already fixed one rung up, where its own note says «the product printed our silence as the platform's». Meta implements `ReportsEntityGrains`, so the spend has been sitting in `entity_daily_metrics` keyed by the ad that ran the creative, underneath a card reading «—» | A creative with no rows of its own reports what its ADS reported — never blended with a native creative grain, summed across ads that share it, nothing attributed from an ad with no creative, and a metric the ad grain has no column for left ABSENT rather than turned into a fabricated zero. Separately, both creative tables carried an unread `is_demo` column while `MetricsAggregator` had guarded the equivalent against «invented money inside a real total»; the same derived policy now applies — this PR | yes — 9638b85c (#414) | yes — the main run completed against exactly this commit (`9638b85c`) and the API answers | no | **There is nothing to grep and nothing to see.** The fix is backend-only, so no served bundle carries a string that proves it, and the figures it restores cannot appear without a real provider account: every credential on this install is a seeded placeholder. What is owed is the owner opening `/app/content` on a connected Meta or Google account and reading a spend that is not «—», then reconciling it against the platform's own reporting for the same window | BLOCKED_OPERATIONAL_EVIDENCE — a connected real account is the only thing that can show this |
| 94h | P0 | Content — cross-surface reconciliation |  Traced provider → ingestion → grain → aggregation → presenter → card before touching any React. `AccountMetricsSyncer` fetches creative-level insights behind `instanceof ReportsCreativeInsights` and Snapchat is the ONLY implementor, so Meta, Google, TikTok, LinkedIn and X never write a `creative_daily_metrics` row — the one table the content library reads. The failure is silent by design: the call is wrapped in a swallow because «creative numbers are an enrichment» | `/app/content` cards, popup, Content Analytics | CONTENT-SPEND-ALWAYS-001; CONTENT-KPI-COVERAGE-002 | IMPLEMENTED_NOT_VERIFIED | Rung 2/3, not rendering. The same `instanceof` shape this codebase already fixed one rung up, where its own note says «the product printed our silence as the platform's». Meta implements `ReportsEntityGrains`, so the spend has been sitting in `entity_daily_metrics` keyed by the ad that ran the creative, underneath a card reading «—» | A creative with no rows of its own reports what its ADS reported — never blended with a native creative grain, summed across ads that share it, nothing attributed from an ad with no creative, and a metric the ad grain has no column for left ABSENT rather than turned into a fabricated zero. Separately, both creative tables carried an unread `is_demo` column while `MetricsAggregator` had guarded the equivalent against «invented money inside a real total»; the same derived policy now applies — this PR | yes — 9638b85c (#414) | yes — the main run completed against exactly this commit (`9638b85c`) and the API answers | no | **There is nothing to grep and nothing to see.** The fix is backend-only, so no served bundle carries a string that proves it, and the figures it restores cannot appear without a real provider account: every credential on this install is a seeded placeholder. What is owed is the owner opening `/app/content` on a connected Meta or Google account and reading a spend that is not «—», then reconciling it against the platform's own reporting for the same window | BLOCKED_OPERATIONAL_EVIDENCE — a connected real account is the only thing that can show this |
| 67 | EXT | Meta structure sync refused by the provider | Forced sync returns `(#200) Ad account owner has NOT grant ads_management or ads_read permission` — the same account synced `records=58` earlier the same day | integrations; /app/content | INTEGRATION-META-001 | INTEGRATION-META-001=**BLOCKED_EXTERNAL_CREDENTIALS** | **CLASSIFICATION CORRECTED 2026-09-15 — this was a blanket applied to a path never proven blocked.** The owner ran a REAL Meta sync and it reached the application and the database, failing there on `SQLSTATE[22001] value too long for varchar(255)`. That is an internal defect of ours, and it MASKS whatever Meta actually answered: the exception replaces the provider's result, so a `(#200)` read from the run log may be the last thing that got recorded rather than what happened on that run. A provider result our own exception overwrote is not evidence about the provider. The blocker in hand is therefore INTERNAL and executable — INTEGRATION-ERROR-CONTRACT-001, the `text` columns and the single `ProviderErrorText` writer. Meta is re-classified only after that ships, a real sync is re-run, and the ORIGINAL provider outcome is captured along with whether fresh campaigns and metric rows are written **ANSWERED 2026-09-15 18:36 UTC, from a sync that ran AFTER the fix deployed.** #425 deployed at 18:17:54; the scheduled Meta insight sync at **18:30:07** is the first through the `text` columns and the single writer, and it recorded Meta's OWN answer whole: `(#200) Ad account owner has NOT grant ads_management or ads_read permission, refer to … · code 200 · trace A-RzlwKh7wnTDCw4BDCT64U`. The trace id is captured here for the FIRST time — which is itself the proof the fix worked, because that string is 296 characters and could not have been written to a varchar(255) at all. **And the arithmetic names the cause, which was ours: #422.** Meta's sentence alone is 253 characters and fitted with two to spare; the identifiers #422 appended to make a refusal actionable took it to 296 and overflowed. The right change, shipped without checking whether the column could hold it — and the exception then replaced the provider's answer, so the screen reported PostgreSQL where it should have reported «grant ads_read». No fresh rows: raw 0, stored 0, and downstream still 317 daily_metric rows with latest 2026-09-06. Structure runs fail with the same `(#200)`. Classification, now EVIDENCED rather than assumed: PERMISSION_REVOKED → BLOCKED_EXTERNAL_CREDENTIALS — the grant is the owner's to make on `act_3493018704182532` in Meta Business settings, and no code change can reach it **AND ASKING META DIRECTLY SAYS SOMETHING THE RUN LOG COULD NOT, 2026-09-15 20:02 UTC.** The `Meta Access Probe` — read-only, `/me/permissions`, nothing rotated — reports for connection `4e0f61a3`: `stored status: connected · last error: — · last success: NEVER`, and from Meta `HTTP 400 · code 100 · subcode 33 · GraphMethodException · «Unsupported get request. Object with ID 'me' does not exist, cannot be loaded due to missing permissions, or does not support this operation» · fbtrace_id AcwoPtG2o1XG4OIrn0Ok9Dt`. That is NOT the `(#200) ads_read` the syncs report. `(#200)` is an ad account declining to share; code 100/33 on `/me` is the token failing to identify itself at all — the shapes that produce it are an app or system-user token where a user token is expected, or a token no longer valid. **So the remedy is probably not only a permission grant.** A connection that has NEVER succeeded and stores NO error, while the UI shows it as «connected», is more likely to need re-authorising from scratch than to need a checkbox. Recorded as evidence and not as a diagnosis: this probe reads one connection, and which connection carries the Project 1 binding is not established here | n/a | n/a | n/a | n/a | Owner re-authorises the Meta connection (fresh consent with a user token) AND grants `ads_read`/`ads_management` on `act_3493018704182532`; the probe and the next scheduled sync then say whether either was the blocker | BLOCKED_EXTERNAL_CREDENTIALS |
| 66 | GOV | Production Truth Audit open until every observation is checked against the running product | — | all | PRODUCTION-TRUTH-AUDIT-001 | PRODUCTION-TRUTH-AUDIT-001=IN_PROGRESS | — | n/a | n/a | n/a | no | This ledger is the checklist | — |
| 95 | P0 | Content metrics are fundamentally inconsistent in Production — sometimes the KPIs appear but Spend is missing, sometimes Spend appears and the other KPIs disappear | The same creative, same period, same scope shows a different set of figures depending on which surface is read | /app/content (card, popup, Content Analytics) and the report surfaces it feeds | CONTENT-SPEND-ALWAYS-001; CONTENT-KPI-COVERAGE-002; CONTENT-AD-DELIVERED-001; CONTENT-DETAIL-MODAL-001 | CONTENT-SPEND-ALWAYS-001=IMPLEMENTED_NOT_VERIFIED<br>CONTENT-KPI-COVERAGE-002=PARTIAL<br>CONTENT-AD-DELIVERED-001=BLOCKED_OPERATIONAL_EVIDENCE<br>CONTENT-DETAIL-MODAL-001=IMPLEMENTED_NOT_VERIFIED | ESTABLISHED — two readers of one creative, consulting different sources. Walked before anything was touched, with `content:reconcile`: nine rungs on one subject and window, naming where they disagree. `CreativeMetrics::totalsFor` — the library's headline strip — was a SECOND SQL projection over `creative_daily_metrics` with no ad-grain fallback and no demo policy, while `forCreatives`, which every card reads, has both. `AccountMetricsSyncer` asks for creative-level insights behind `instanceof ReportsCreativeInsights` and Snapchat is the only implementor, so on Meta, Google, TikTok, LinkedIn and X the cards carry spend and the strip directly above them returned null — the figures appearing and vanishing in one viewport. Four further instances of the same shape, each proved fail-first: `aggregate()` iterated the creative table's column list, so groups, the format comparison and Content Analytics lost `leads`, `sign_ups`, `installs`, `app_opens` and `page_views`; the card rendered its fixed Spend cell with NOTHING beside it and no sentence, because the «no displayable metrics» panel was gated on `headline_metrics.length === 0` while `supportable()` returns `['spend']` in exactly that state; the popup and Content Analytics' figures grid read money through the converted column alone, so a withheld figure — FX-001's design, and the state of every row on a USD account with no USD→SAR rate — printed «Not provided» over money the platform sent; and `ObjectiveFamily::Engagement` named `engagement_rate` and `cpe` while `derive()` computed neither | yes — `totalsFor` delegates to `forCreatives` + `aggregate()` rather than gaining a UNION, because a wider query would have closed today's two divergences and kept the mechanism that generates them. `aggregate()` sums both grains and pools the money's provenance; `besideSpend()` answers the card grid and its empty-state gate as one; `creativeFigureText` is the ONE reader every Content surface now asks, after a sweep of every `metricState` call site that also NAMED the four already correct so nobody re-audits them; `engagement_rate` and `cpe` are derived. Guards: nine service cases, the endpoint case asserted against the CARDS rather than a literal, the strip added as a fifth surface to `CreativeSurfaceReconciliationTest` (300 assertions), the «a family may not NAME a verdict it cannot produce» property over every family, and owner rows 9/10 as a property over 576 preview readings in both languages. Every one proved non-vacuous by injection — including my own first draft of the preview property, which exempted a playable film and so could not catch the historical blank box | yes — **cf582970** (#436, the fix and the walk) and **4fa2555e** (#438, the route to production), both six of six green on a head rebased onto current main | yes — and proved two ways rather than by a green deploy job, because the last deploy of this kind reported green while the SSH action hit its 600s limit and production kept serving the previous commit. The served bundle MOVED: `/assets/index-3vaI5bmM.js` → `/assets/index-C_wyiNaW.js`. And the BACKEND is proved by a capability rather than a string: `content:reconcile` RAN on the VPS, and that command does not exist before cf582970. A bidirectional string check was not available for the frontend half — these changes add no user-visible copy and remove none — which is worth saying rather than leaving somebody to wonder why the usual check is missing | no | The owner opening `/app/content` on a connected account and reading spend and the objective's own result together, reconciled against the platform's own reporting for the same window. **WHAT PRODUCTION HAS ALREADY ESTABLISHED, read-only, run `35049110777` on the live Snapchat account bound to Project 1, window 2026-08-18 → 2026-09-16:** 1,539 creatives, 199 with figures — 160 reported at creative grain and **39 summed from their ADS**, which the BEFORE reading predicted exactly («no rows; its ad DID run in window : 39»). The strip and the pooled cards both answer **35 figures**; a creative-grain-only strip answers **16**. **The money was NOT short** — all three read 8898.657614, because those ad-grain rows carry the RESULT columns and no spend — so what the strip could not state was nineteen FIGURES: `leads`, `installs`, `sign_ups`, `app_opens`, `page_views`, `reach`, `purchases`, `add_to_cart`, `checkout` and everything derived from them. That is «the other KPIs disappear» measured on the live estate, and it is the MECHANISM closed rather than the acceptance clause — it says nothing about what the owner sees on his screen. Media, same source: page one holds 24 cards, 23 drawable and one `shape_not_fetched`, which carries both a sentence and a badge, so the single blank card on the first screen is explained rather than silent | Only for the figures themselves: every credential on this install is a seeded placeholder, so no provider response can be observed here. The collection-tile and multi-asset INGESTION clauses stay BLOCKED_EXTERNAL_CREDENTIALS on their own rows — those provider shapes are unobservable from here and guessing one would be fabrication |
| 96 | P0 | Reports expose several options and modes, but the reports they produce look effectively the same | Executive Summary and Detailed render the same document with more text; Client and Internal do not differ where configured; changing settings does not change the product | /app/reports — creation, render, snapshot, share, export | REPORT-PRODUCT-MODEL-001; REPORT-ANALYTICAL-DEPTH-001; REPORT-DETAIL-PARITY-001; REPORT-FORM-001; REPORT-FORM-002; REPORT-FORM-003 | REPORT-PRODUCT-MODEL-001=PARTIAL<br>REPORT-ANALYTICAL-DEPTH-001=PARTIAL<br>REPORT-DETAIL-PARITY-001=PARTIAL<br>REPORT-FORM-001=VERIFIED<br>REPORT-FORM-002=VERIFIED<br>REPORT-FORM-003=VERIFIED | ESTABLISHED, and it is not «two React conditionals». The link builder writes the operator's form choice to `report_shares.form` and created the `reports` row with NO form, so `reports.form` fell to its `NOT NULL DEFAULT 'detailed'` — and `LiveReportService` read that column. Proved on a builder-shaped link: share.form=`executive_summary`, `formOr()` told the PAGE `executive_summary`, the PAYLOAD was composed as `detailed`. Two sources of truth for one setting, disagreeing silently on every link the product itself creates, so the only backend lever distinguishing the two products (the roster cap in `ReportAds`) was PERMANENTLY OFF in production. The visible half was that the form axis was two conditionals in one React component, making a summary «the detailed report with two blocks withheld» — the wrong way round. Contrast proving it was not a missing capability: the snapshot deck path already differentiated correctly (`ReportTemplateEngine::defaultConfig(..., $form)`, `ClientReportView::EXECUTIVE_SLIDE_TYPES`). Separately the export ignored the form entirely — `withoutHiddenSections` dropped a section only when its slide was present AND `visible=>false`, and a summary OMITS those slides — so both forms exported byte-identical files | yes — PR #435: `ReportComposition`, `LiveReportService::formFor()`, builder persists the form, `LiveSharedReport` composition, `ReportExporter` honours the document's own composition + Funnel/Budget sections, `AdsSlide` roster count-only on paginated slides, comparison slide de-duplicated | yes — #435, main `1f2769e1` | yes — the served bundle moved to `/assets/index-xs8nQCPI.js` and carries four strings unique to this merge («في التقرير التفصيلي فقط», «In the detailed report only», «القائمة الكاملة في التقرير التفاعلي», «interactive report and in the exported files»), and the two client-diagnostic strings this merge REMOVED («نموذج/نافذة الإسناد», «حالة التقرير») are gone from it. The asset hash is the proof, not the deploy job — a green deploy has reported success here while the VPS kept serving the previous commit. This confirms the FRONTEND build only: the health endpoint publishes no commit, so the backend half is inferred from the shared deploy and is not independently proven | no — and it needs the Owner. Creating the two shares requires an authenticated session; there is no unauthenticated path to a live link, and the reference share `RESUME_STATE` records for browser acceptance now answers 404 on both its endpoints, so no Production share is reachable to read. Guessing a token would be enumeration against a client surface | MEASURED on the live link, same project/window/scope, two shares differing only in `form`. BEFORE: Executive 43 blocks / 3043px / 314 words against Detailed 45 / 3189px / 325 — the two products differed by +2 blocks, +146px (4.8%) and ELEVEN WORDS. AFTER: Executive 30 / 2158px / 204, Detailed unchanged to the pixel at 45 / 3189px / 325 — +15 blocks, +1031px (48%), +121 words. Exports BEFORE: executive and detailed CSVs byte-identical apart from their own timestamp (3411 bytes, 61 lines each); AFTER exec/client 3634b vs detailed/client 3874b (funnel section), internal adds campaigns. Client PDF BEFORE: could not be produced at all — `layout_validation_failed`, proven pre-existing on b7e98354; AFTER both forms render (detailed 1,172,894b, summary 713,339b), 0 failing pages, worst fitScale 0.92. Arabic text layer verified with Apple PDFKit on the pipeline output: 0 presentation forms, 0 Arabic-Indic digits, «الإنفاق»/«الملخص»/«المنصات»/«التقرير» all searchable. STILL OPEN: Production verification on the deployed site; the deck/snapshot composition beyond the two slides fixed here; and client-vs-internal on LIVE links, which is an ABSENT axis rather than a broken one — `LiveReportBuilderController` pins the audience to `client` deliberately, because `ReportShareController` already refuses 422 to share an internal report and `ClientEntityBoundary` strips campaign identity from every live payload, so an «internal live link» would mean restoring what the Owner removed | — |
| 97 | P0 | The client PDF export could not be produced at all — pressing PDF returned a failed export and no file | `ReportExporter::render($report, 'pdf')` throws `layout_validation_failed` for BOTH forms, so no client PDF exists for a realistic multi-platform report | /app/reports — export; `/reports/print/{token}`; the client's downloaded PDF | REPORT-EXPORT-FUNCTIONAL-001; REPORT-DETAIL-PARITY-001; CLIENT-DIAGNOSTIC-SEPARATION-001 | REPORT-EXPORT-FUNCTIONAL-001=IMPLEMENTED_NOT_VERIFIED<br>CLIENT-DIAGNOSTIC-SEPARATION-001=PARTIAL | ESTABLISHED, and PRE-EXISTING — proven independently rather than assumed. With every file of the #435 Reports diff reverted to b7e98354, the identical failure reproduces byte for byte: `layout_validation_failed … page 5: unreadable(0.847)`. The deck auto-fits a slide and fails it below a 0.85 readable floor. TWO slides were over that floor. **Page 12, the ads slide: fitScale 0.166 with 406 elements CLIPPED** — `AdsSlide` rendered the ranked ads AND the complete sixty-row creative roster, and the deck reuses `SlideBody` with the on-screen report, so a table that merely scrolls in a browser had to fit one landscape A4 page. **Page 9, the platform comparison: fitScale 0.847**, three thousandths under the floor — the slide stated spend-by-platform FOUR times (a ranking bar chart, a donut of the same spend, a spend column and a share column in the table beneath them), which is handoff §10 «too much card/box repetition» and §11 «when comparison is the job, prefer a clean table». Recorded because the measurement corrected the reasoning: removing the bar chart alone changed NOTHING, because the two charts shared a row and the height was the donut's either way. Two §13 «do not expose implementation internals» leaks were found in the same document: the client's own comparison table printed `google`, `snapchat`, `tiktok` — our stored provider keys — down its first column while the donut beside it said «جوجل»; and the export manifest wrote «Data source: daily_metrics», the name of a database table, into the file a client downloads and keeps | yes — PR #435: roster states its count on a paginated surface and prints its list where a surface can scroll (`countOnly`, threaded from `PrintReport` through `SlideBody`); comparison slide de-duplicated and laid out donut-beside-table; `plat()` used for the platform column; manifest internals restricted to an internal audience | yes — #435, main `1f2769e1` | yes — the served bundle moved to `/assets/index-xs8nQCPI.js` and carries four strings unique to this merge («في التقرير التفصيلي فقط», «In the detailed report only», «القائمة الكاملة في التقرير التفاعلي», «interactive report and in the exported files»), and the two client-diagnostic strings this merge REMOVED («نموذج/نافذة الإسناد», «حالة التقرير») are gone from it. The asset hash is the proof, not the deploy job — a green deploy has reported success here while the VPS kept serving the previous commit. This confirms the FRONTEND build only: the health endpoint publishes no commit, so the backend half is inferred from the shared deploy and is not independently proven | no — and it needs the Owner. Creating the two shares requires an authenticated session; there is no unauthenticated path to a live link, and the reference share `RESUME_STATE` records for browser acceptance now answers 404 on both its endpoints, so no Production share is reachable to read. Guessing a token would be enumeration against a client surface | MEASURED through the real pipeline with the layout gate ENABLED. BEFORE: both forms threw, no PDF at all. AFTER: detailed/client 1,172,894 bytes and executive_summary/client 713,339 bytes, 16 pages, **0 failing pages, worst fitScale 0.92** (was 2 failing, one at 0.166 with 406 clipped). The two forms now differ by 40% of file size in the PDF as well as on screen. Arabic text layer verified on the pipeline output with **Apple PDFKit — deliberately a different tool from the pikepdf that performs the normalisation, so the check is not the fixer marking its own work**: 0 presentation forms, 6,980 Arabic base letters, 1,695 Latin digits, 0 Arabic-Indic digits, and «الإنفاق» «الملخص» «المنصات» «التقرير» all searchable as typed. **THE SLIDE AUDIT THIS ROW ASKED FOR IS DONE, AND IT FOUND TWO MORE PRE-EXISTING DEFECTS.** The stated gap was that «the remaining deck slides have not been audited at other data shapes». Five shapes were seeded into a clean database — 1 platform/1 objective, 4 platforms, 6 platforms/5 objectives, withheld money, and a 240-creative estate — and all TEN renders (5 shapes x 2 forms) were driven through the real exporter with the layout gate ENABLED, never through `report-print.mjs` directly. Nine passed on merged main. The 240-creative shape failed BOTH forms identically — `page 7 / page 14: clipped(108) unreadable(0.355)` — and the identical signature on both forms is what proved it was NOT the roster, which #435 had already made count-only. It is the ADS slide: `ReportAdsSection` renders one block per objective group, and five groups do not fit a fixed A4 landscape page. The demo estate masked it because its creatives carry no figures, so that block never rendered at all — which is why the original fix was verified against data where the defect could not appear. **Bounded to one group with the remainder STATED**, not dropped: «4 more objective(s) are shown in the interactive report and the exported files», because a bound that goes quiet turns «the objectives this page had room for» into «the objectives that performed». The bound is on GROUPS and not on cards, settled by measurement rather than argument — two groups of three and two groups of two both measured 0.80, since three cards and two occupy the same single row of a three-column grid. That is the second time on this slide family that a change which looked like it must reduce height reduced nothing. **The second defect surfaced only once the first was fixed**, and it blocked the export outright: `ascii_mappings_preserved=false`. `fix-arabic-textlayer.py` states three times that it folds «the Arabic PF ranges» only, and `_fold_hex` applied NFKC to EVERY destination unconditionally — NFKC is a COMPATIBILITY fold, so U+2026 HORIZONTAL ELLIPSIS became three full stops. Tailwind `truncate` writes a real U+2026 glyph, so a long creative name produced an ASCII mapping that did not exist before the sweep, and the validator correctly refused the file. Fail-closed worked exactly as designed — the validator caught the normaliser — and the consequence was that such a report could not be exported AT ALL. The fold is now gated on the PF ranges, matching the module's own contract. **Its own test could never have caught this**: the fixture asserted «ASCII untouched» using `A` and `0`, both fixed points of NFKC, so it passed while the invariant was broken — a fixture that could not fail. It now carries U+2026, the character production actually produced, asserted on the CMap rather than through the ASCII counter, which cannot see a non-ASCII source. Proved by injection at both ends: ungating the fold reproduces `ascii_mappings_preserved: False` with `cmaps_modified` 6 against 5, and removing the group bound fails the slide test. AFTER, all ten renders pass and the form difference holds at every shape — 481K/689K, 577K/968K, 591K/1,101K, 596K/1,107K, 698K/1,225K. PDFKit on the 240-creative pair: executive 10 pages / 7,515 chars, detailed 18 pages / 13,738 chars, **0 presentation forms on both**, «الإنفاق» searchable on both and «الظهور» «النقرات» on the detailed — so the «materially different» products are visible in PDFKit's own reading of the delivered file, not only in configuration. STILL OPEN: Production verification on the deployed site, which needs the Owner's authenticated session | — |
| 98 | GOV | A Production deploy cannot be independently proven, so every Production verification on this project rests on an inference | `/api/v1/health` publishes no commit, version or build identifier. The only available proof that a merge reached Production is the frontend asset hash, which says nothing about the backend | Deployment; every row whose acceptance is «verified on Production» | UNMAPPED_OWNER_DEFECT | n/a — no Matrix row covers deploy provenance | ESTABLISHED. Three separate verifications tonight had to reason around it. #432's Marketing fix was confirmed by the served HTML gaining both font preloads; #436's Content fix by the bundle hash moving from `index-3vaI5bmM.js` to `index-C_wyiNaW.js`; #435's Reports fix by the stronger bidirectional check — the bundle CARRIES four strings unique to that merge and the two strings it REMOVED are GONE, which a one-directional check could not distinguish from any other deploy. All three prove the FRONTEND build only. The backend half is inferred from the fact that one pipeline deploys both. That inference has already been wrong in a recorded way: a green «Deploy Production» job reported success while the SSH action hit its 600s limit and the VPS kept serving the previous commit, so the job's own status is not the signal either. The Content child found the one counter-example that does prove a backend build — `content:reconcile` RAN on the VPS and that command does not exist before cf582970 — but proving a deploy by whether a new command happens to exist is a coincidence of that change, not a mechanism | no — recorded, not fixed. The remedy is a commit or build identifier published by the health endpoint, which is a small change but belongs to whoever owns the deploy pipeline and needs agreement on the mechanism rather than a unilateral five-line patch | no | no | n/a | A deployed commit can be read back from Production without inference — an endpoint that names the build, checked the way the asset hash is checked now. Until then every «Production verified» claim on a backend change carries this caveat and should say so, as rows 96 and 97 already do | — |
| 99 | GOV | The E2E gate loses exactly one test per full-suite run on both non-chromium engines, blocking PRs for reasons unrelated to their diffs | Six failures in one night, each 596 passed / 1 failed / 5 skipped. Never two failures, never zero. Chromium has never failed | CI — `gate (firefox)` and `gate (webkit)`, branch protection on every PR | UNMAPPED_OWNER_DEFECT | n/a — no Matrix row covers gate reliability | NOT ESTABLISHED, and deliberately left open rather than called flake. The failures, in order: #434 webkit `request-vertical.spec.ts:28` (`page.goto: WebKit encountered an internal error`); #437 webkit `portal-audit.spec.ts:105` (`walkRail`, `helpers.ts:698`, `locator('main')` absent 45s); #437 rebased, webkit `agency-portal.spec.ts:52` (same helper, same line); #439 webkit `campaigns-overview-first.spec.ts:137` (`landing-attention` not visible, 5s); #437 rebased again, webkit the SAME `landing-attention` AND firefox `campaigns-linking.spec.ts:54` (`toHaveURL`, 5s) in one run. Two readings were tried and discarded on evidence: «one bad helper» died when the fourth failure was an unrelated spec, and «a webkit quirk» died when firefox produced the identical signature. What survives: four of six are an element that never appeared, and `landing-attention`'s assertion is BRANCHED — it read a NON-ZERO strip count and then could not find the element, so the data had arrived and the branch above had already consumed it. That is the shape of a render race, not a wrong number. **The possibility that must not be closed without proof: a genuine race in `features/campaigns/CampaignsPage.tsx`, where `landing-attention` is fed by `attentionFlags` and `metricsByCampaign` — two async sources joined in a memo — which chromium's timing happens to hide. If that is the cause it is an Owner-visible defect and not a test problem.** Also unexplained: the two newest failures waited 5000ms for elements behind a network round trip while the earlier pair waited 45000ms | no — assigned to an isolated workstream with all six data points. A bare timeout increase is not an acceptable fix | no | no | n/a | A root cause proved by reproduction, and a fix proved proportionally — repeated full runs or an injected race that fails before and passes after. One green run proves nothing about a one-in-597 failure. Until then the standing rule holds: retry once, and a spec that fails twice is handed over rather than retried again | — |
## Reading the P0 media block together

Rows 1–12 are one defect family and were reported as one symptom: **the content library shows
nothing where an ad should be.** They are listed separately because they have different causes and
close at different times, not because the owner reported twelve things.

The two proven root causes so far, both on the live estate rather than in a fixture:

1. `CreativePresenter` read a creative's image as `asset_url ?? preview_url`. `preview_url` is
   Meta's `preview_shareable_link` — a link to a page. Six of twelve first-page Meta cards fetched
   `fb.me/…`, got HTTP 200 with `text/html`, and decoded nothing.
2. Meta reports `object_type: SHARE` for a dynamic product ad, so those same six were typed as
   images with a missing picture. `object_story_spec.template_data` is the marker, and it was
   already being fetched.

Neither was visible from a database column, a presenter payload or a component test, which is why
rows 1–12 stay open until the owner's own `/app/content` is seen.
